# Fase 16 (fixture) - Context

Fixture sintética que trava a contagem correta de `planCount` na presença de
artefatos que se parecem com plano mas não são: `UI-SPEC`, `VALIDATION` e
`DISCUSSION-LOG` não podem ser contados como plano nem como summary.
