---
phase: 05-comunidade
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - package.json
  - package-lock.json
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
autonomous: false
requirements: [DIST-03]
user_setup: []
must_haves:
  truths:
    - "The Tauri updater and process plugins are installed (JS + crate) and registered in the Rust builder chain, so the app can check for, verify, and install signed updates and relaunch itself"
    - "The webview is granted ONLY the per-command updater/process permissions the frontend actually calls (check, download-and-install, restart) — never the plugin's catch-all bundle, preserving the codebase's granular-capability discipline"
    - "cargo check passes with the two plugins registered and the three capability grants applied"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "tauri-plugin-updater + tauri-plugin-process crate dependencies (loose \"2\" pin)"
      contains: "tauri-plugin-updater"
    - path: "src-tauri/src/lib.rs"
      provides: "Builder registration of the updater + process plugins"
      contains: "tauri_plugin_updater"
    - path: "src-tauri/capabilities/default.json"
      provides: "Granular updater/process permission grants"
      contains: "updater:allow-check"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/Cargo.toml"
      via: "tauri_plugin_updater::Builder::new().build() requires the crate dependency"
      pattern: "tauri_plugin_updater"
    - from: "src-tauri/capabilities/default.json"
      to: "src-tauri/Cargo.toml"
      via: "updater:allow-* / process:allow-restart permission identifiers come from the plugin crates' autogenerated schemas"
      pattern: "process:allow-restart"
---

<objective>
Install and wire the Tauri auto-update backend (DIST-03 foundation): the
`@tauri-apps/plugin-updater` + `@tauri-apps/plugin-process` JS packages and their crate
counterparts, registered in `lib.rs` exactly like every Phase-4 plugin, with granular
per-command capabilities.

## Phase Goal (user story — derived from ROADMAP Goal + Success Criterion 3)

**As a** GSD Cards user, **I want to** have the app able to check for, verify, and install
new versions and relaunch itself, **so that** I stay on the latest release without hunting
for downloads.

Purpose: This plan installs the one genuinely new mechanism of Phase 5 (signed
auto-update) and gates the two npm packages behind a package-legitimacy checkpoint. It
provides the plugin surface that plan 05-03 (frontend wrapper/UI) and plan 05-04
(config/endpoint/CI) build on.
Output: two installed plugins, two `.plugin(...)` registrations, three granular capability
grants, `cargo check` green.

## Artifacts this phase produces (this plan)

- `package.json` / `package-lock.json` — `@tauri-apps/plugin-updater`, `@tauri-apps/plugin-process`
- `src-tauri/Cargo.toml` / `Cargo.lock` — `tauri-plugin-updater`, `tauri-plugin-process`
- `src-tauri/src/lib.rs` — two additive `.plugin(...)` calls
- `src-tauri/capabilities/default.json` — three granular permission grants
</objective>

<execution_context>
@/home/user/gsd-cards/.claude/gsd-core/workflows/execute-plan.md
@/home/user/gsd-cards/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-comunidade/05-CONTEXT.md
@.planning/phases/05-comunidade/05-RESEARCH.md
@.planning/phases/05-comunidade/05-PATTERNS.md
@src-tauri/src/lib.rs
@src-tauri/Cargo.toml
@src-tauri/capabilities/default.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for the updater + process npm packages</name>
  <what-built>
    Package-legitimacy gate for the two npm packages this plan installs, per protocol —
    both were flagged SUS in `05-RESEARCH.md ## Package Legitimacy Audit`, and the SUS
    verdict is a sandbox `api.npmjs.org`-download-telemetry gap, NOT a legitimacy finding.
    Both are official `tauri-apps` packages from `github.com/tauri-apps/plugins-workspace`
    — the identical repo/org as `plugin-store`, `plugin-notification`, `plugin-fs`,
    `plugin-dialog`, and `plugin-opener` already trusted across Phases 1-4:
      - `@tauri-apps/plugin-updater@^2.10.1` (crate `tauri-plugin-updater` OK on crates.io, no gate)
      - `@tauri-apps/plugin-process@^2.3.1` (crate `tauri-plugin-process` OK on crates.io, no gate)
  </what-built>
  <how-to-verify>
    Confirm each npm package is the official tauri-apps plugin before the install task runs:
    1. Open https://www.npmjs.com/package/@tauri-apps/plugin-updater — publisher is the
       tauri-apps org, repo `tauri-apps/plugins-workspace`, latest ~2.10.1.
    2. Open https://www.npmjs.com/package/@tauri-apps/plugin-process — same org/repo, latest ~2.3.1.
    3. Confirm neither has a suspicious postinstall script and both match the versions in
       `05-RESEARCH.md ## Standard Stack`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name a substitution/concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install + register the updater/process plugins with granular capabilities</name>
  <files>package.json, package-lock.json, src-tauri/Cargo.toml, src-tauri/Cargo.lock, src-tauri/src/lib.rs, src-tauri/capabilities/default.json</files>
  <read_first>
    - `src-tauri/src/lib.rs` lines 19-35 (existing `.plugin(...)` chain + the WHY doc-comment convention above each plugin, PATTERNS "lib.rs (+2 .plugin(...))")
    - `src-tauri/Cargo.toml` (existing `tauri-plugin-*` = "2" lines, PATTERNS "Cargo.toml (+2 deps)")
    - `src-tauri/capabilities/default.json` line 4 header + the `store:allow-*`/`notification:allow-*` block (PATTERNS "capabilities/default.json (+3 perms)")
    - `05-RESEARCH.md` Pitfall 2 (granular vs catch-all) + `## Code Examples` (lib.rs registration + granular capability identifiers)
  </read_first>
  <action>
    Run `npm install @tauri-apps/plugin-updater @tauri-apps/plugin-process` (versions per
    the CONTEXT "Auto-update" decision / RESEARCH Standard Stack). Add `tauri-plugin-updater = "2"` and
    `tauri-plugin-process = "2"` to `src-tauri/Cargo.toml` under `[dependencies]`, matching
    the loose `"2"` pin every sibling `tauri-plugin-*` already uses. In `src-tauri/src/lib.rs`,
    append `.plugin(tauri_plugin_updater::Builder::new().build())` and
    `.plugin(tauri_plugin_process::init())` to the existing `tauri::Builder::default()` chain
    (after the notification plugin, before `.manage(...)`), each preceded by a doc-comment
    explaining WHY and what capability it needs, following the file's own convention. In
    `src-tauri/capabilities/default.json`, add exactly three granular grants —
    `updater:allow-check`, `updater:allow-download-and-install`, and `process:allow-restart`
    — matching the only two updater calls the frontend makes (check + downloadAndInstall)
    plus the relaunch, per RESEARCH Pitfall 2 / the CONTEXT capability-granularity rule.
    Do NOT grant the plugin's
    all-commands catch-all bundle permission (it violates the file's own header rule
    "nunca o bundle catch-all"); extend the header `description` with a sentence documenting
    the updater/process grants' scope, matching how the store/notification grants are each
    documented inline.
  </action>
  <acceptance_criteria>
    - `cargo check --manifest-path src-tauri/Cargo.toml` passes
    - `npm run typecheck` passes
    - `grep -q "tauri-plugin-updater" src-tauri/Cargo.toml` and `grep -q "tauri-plugin-process" src-tauri/Cargo.toml`
    - `grep -q "tauri_plugin_updater" src-tauri/src/lib.rs` and `grep -q "tauri_plugin_process" src-tauri/src/lib.rs`
    - `grep -Eq "updater:allow-check|updater:allow-download-and-install" src-tauri/capabilities/default.json` and `grep -q "process:allow-restart" src-tauri/capabilities/default.json`
    - the catch-all bundle grant is absent: `grep -c 'updater:default' src-tauri/capabilities/default.json` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml && npm run typecheck</automated>
  </verify>
  <done>Both plugins are installed and registered; the three granular capabilities are granted (no catch-all bundle); cargo check and typecheck are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/crates registry → local dependency tree | Third-party packages enter the build; a malicious/typosquatted package would run at install/build time |
| webview (frontend JS) → updater/process plugin commands | The capability grant defines exactly which privileged native commands the frontend may invoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm installs of `@tauri-apps/plugin-updater` + `@tauri-apps/plugin-process` | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) before install; both are official tauri-apps packages from the same repo as five plugins already trusted in Phases 1-4; SUS verdict is a sandbox telemetry gap, not a finding |
| T-05-03 | Elevation of Privilege | `src-tauri/capabilities/default.json` grant surface | medium | mitigate | Grant only the three per-command permissions the frontend actually calls (check, download-and-install, restart); refuse the plugin's all-commands catch-all bundle, keeping the update-install privilege surface minimal (RESEARCH Pitfall 2) |
</threat_model>

<verification>
- Package-legitimacy checkpoint approved before install
- `cargo check --manifest-path src-tauri/Cargo.toml` passes with both plugins registered
- `npm run typecheck` passes
- Granular grants present; catch-all bundle grant absent (`grep -c 'updater:default' ... == 0`)
</verification>

<success_criteria>
The updater + process plugins are installed and registered, the webview holds only the
three narrow permissions the frontend uses, and the Rust build compiles — providing the
backend surface plans 05-03 and 05-04 depend on.
</success_criteria>

<output>
Create `.planning/phases/05-comunidade/05-02-SUMMARY.md` when done
</output>
