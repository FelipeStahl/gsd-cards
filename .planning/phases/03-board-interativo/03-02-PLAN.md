---
phase: 03-board-interativo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [ACT-03]
user_setup: []
must_haves:
  truths:
    - "`strip-ansi@7.2.0` is present in package.json dependencies and installed, and `import stripAnsi from \"strip-ansi\"` type-checks in the renderer TypeScript project."
    - "The `strip-ansi` legitimacy was human-confirmed (npmjs.com/package/strip-ansi) before install — the automated audit verdict was SUS solely due to a download-telemetry gap, not a content/behavior red flag (A3)."
  artifacts:
    - package.json
    - package-lock.json
  key_links:
    - "strip-ansi → consumed by src/pty/activity.ts (Plan 03) to ANSI-strip the rolling buffer before classification"
---

<objective>
Isolated, human-gated preparation of the one new dependency this phase needs: `strip-ansi@7.2.0`, used by the terminal-activity classifier (Plan 03) to strip ANSI escape codes before running the busy/awaiting regexes. Kept as its own tiny plan so the blocking human legitimacy checkpoint never blocks (and is never entangled with) the classifier implementation, per the package-legitimacy protocol and the "checkpoint + implementation should not share a plan" split signal.

Purpose: satisfy the Package Legitimacy Gate — the automated audit flagged `strip-ansi` as SUS (reason: `unknown-downloads`, a sandbox telemetry gap, not a real red flag), so protocol requires a blocking human verification before `npm install`.
Output: `strip-ansi` in `package.json` + lockfile, verified importable.
</objective>

## Phase Goal (user story)

**As a** GSD developer, **I want to** have the app detect when Claude is busy in the terminal, **so that** actions never inject commands into a busy session — this plan lands the ANSI-stripping dependency that classification depends on.

> Note: the ROADMAP `**Goal:**` line for Phase 3 is prose, not canonical `As a / I want to / so that` form; the story above is derived from the Success Criteria. Run `/gsd mvp-phase 3` to formalize the ROADMAP line.

## Artifacts this phase produces (full phase inventory)

New: `src/planning/actions.ts` (`derivePhaseAction`, `sanitizePhaseId` — Plan 01), `src/planning/injection.ts` (`resolveInjection` — Plan 04), `src/components/PhaseCardAction.tsx` (Plan 01), `src/components/ActivityDot.tsx` (`ActivityDot`, `TONE_BY_ACTIVITY` — Plan 04), `src/pty/activity.ts` (`classifyActivity`, `appendToRollingBuffer`, `TerminalActivity`, `BUSY_MARKER`, `AWAITING_MARKER`, `ROLLING_BUFFER_CAP`, `QUIESCENCE_MS` — Plan 03), `src/components/terminal/useTerminalActivity.ts` (`wireTerminalActivity` — Plan 03), `src/components/terminal/GsdCommandToolbar.tsx` + `src/components/terminal/CommandPalette.tsx` (Plan 05), `src/locales/{pt-BR,en}/commands.json` (Plan 05).
Extended: `src/pty/channel.ts` (`activityHandlers` map + `setActivityHandler`/`clearActivityHandler` — Plan 03), `src/stores/session-store.ts` (`SessionDescriptor.activity` + `setActivity` — Plan 03), `src/i18n.ts` (Plan 05), `board.json`/`terminal.json`, `src/styles/theme.css` (`.status-dot--pulse` — Plan 04).
This plan (03-02) delivers: the `strip-ansi@7.2.0` dependency that `src/pty/activity.ts` imports.

<execution_context>
@/home/user/gsd-cards/.claude/gsd-core/workflows/execute-plan.md
@/home/user/gsd-cards/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-board-interativo/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify strip-ansi before install</name>
  <what-built>Nothing yet — this gate precedes `npm install`. The researcher's Package Legitimacy Audit flagged `strip-ansi@7.2.0` as SUS solely because weekly-download telemetry was unreachable in the research sandbox (`unknown-downloads`), NOT because of any content/behavior red flag: `exists: true`, real repo `github.com/chalk/strip-ansi`, no deprecation, no postinstall script, single transitive dep `ansi-regex@6.2.2`. This checkpoint is never auto-approvable (workflow.auto_advance is ignored for legitimacy gates).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/strip-ansi and confirm: publisher is the `sindresorhus`/`chalk` ecosystem, latest version `7.2.0`, healthy weekly download count (millions — one of the most-depended-upon npm packages), repository `github.com/chalk/strip-ansi`, no deprecation banner.
    2. Confirm the only transitive dependency is `ansi-regex` (also chalk-org), no install scripts.
    3. If anything looks off (unexpected publisher, typosquat name, deprecation, postinstall script), reject and stop — do NOT install.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install strip-ansi@7.2.0`, or describe the red flag to abort.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install strip-ansi@7.2.0 and confirm it is importable</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency list + `"type": "module"` — ESM, compatible with strip-ansi's ESM-only shipping)
    - 03-RESEARCH.md `## Standard Stack` → Supporting (strip-ansi rationale, version verification) + `## Package Legitimacy Audit`
  </read_first>
  <action>
    After the checkpoint is approved, run `npm install strip-ansi@7.2.0`. Confirm `package.json` lists `strip-ansi` at `^7.2.0` (or the exact `7.2.0` if the sibling-dep convention in this repo pins exact) under `dependencies`, and that `package-lock.json` resolves `strip-ansi@7.2.0` + `ansi-regex@6.2.2`. Do NOT add any other package. Add a throwaway import assertion (a one-line `import stripAnsi from "strip-ansi";` used in a temporary scratch check, or rely on Plan 03's `activity.ts` import) to confirm module resolution — but the durable proof is that `npm run typecheck` still passes with the dependency present. Do not create `activity.ts` here (that is Plan 03).
  </action>
  <acceptance_criteria>
    - `node -e "import('strip-ansi').then(m => { if (typeof m.default !== 'function') process.exit(1); })"` resolves and exits 0 (the default export is the strip function).
    - `strip-ansi` appears under `dependencies` in package.json and is resolved in package-lock.json.
    - `npm run typecheck` still passes.
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('strip-ansi').then(m => { if (typeof m.default !== 'function') { console.error('bad export'); process.exit(1); } console.log('strip-ansi ok'); })" && npm run typecheck</automated>
  </verify>
  <done>`strip-ansi@7.2.0` is installed, present in package.json + lockfile, and importable with a function default export; typecheck green.</done>
  <precondition>The preceding `checkpoint:human-verify` was approved (strip-ansi legitimacy confirmed on npmjs.com).</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → renderer bundle | a newly installed package's code is bundled into the app that drives real shells |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | `npm install strip-ansi` | high | mitigate | Package Legitimacy Gate: blocking human verification against npmjs.com/package/strip-ansi before install; SUS verdict was a telemetry gap, not a red flag; never auto-approvable |
</threat_model>

<verification>
- Checkpoint approved by a human before install (blocking, non-auto-approvable).
- `strip-ansi@7.2.0` importable with a function default export; `npm run typecheck` green.
- **Gate applicability note:** no external HTTP API / ORM / identity-transition in this plan — API-coverage / schema-push / assumption-delta detectors are expected to NOT fire.
</verification>

<success_criteria>
- `strip-ansi@7.2.0` present and importable, human-verified legitimate; no other dependency added.
</success_criteria>

<output>
Create `.planning/phases/03-board-interativo/03-02-SUMMARY.md` when done.
</output>
