---
phase: 01-espelho-fiel
plan: 01
subsystem: infra
tags: [tauri, react, typescript, vite, tailwindcss, i18next, vitest, rust, ci]

# Dependency graph
requires: []
provides:
  - Toolchain Rust + MSVC unlocked on the Windows dev machine, documented in CONTRIBUTING.md for all 3 platforms
  - Explicit human approval of the 10 packages outside the pre-approved research audit (Task 2 gate resolved, no substitutions)
  - Executable Tauri v2 + React 19 + TypeScript 7 app scaffold (`npm run tauri dev` opens a native window)
  - Read-only capability allowlist (`src-tauri/capabilities/default.json`) restricted to exactly 6 identifiers, verified against the installed plugin versions' autogenerated permission schemas
  - Design tokens from 01-UI-SPEC.md as Tailwind 4 `@theme` block, light/dark via `prefers-color-scheme`
  - i18next bootstrap with pt-BR/en `common` namespace, key parity enforced
  - CI matrix (windows-latest/macos-latest/ubuntu-latest) running typecheck, vitest, and cargo check
affects: [01-02, 01-03, 01-04, 01-05, 01-06]

# Tech tracking
tech-stack:
  added:
    - "Tauri v2 (@tauri-apps/cli 2.11.4, @tauri-apps/api 2.11.1, crate tauri 2.11.5)"
    - "React 19.2.8 + react-dom + Vite 8.1.5 + @vitejs/plugin-react"
    - "TypeScript 7.0.2 (tsgo)"
    - "Tailwind 4.3.3 + @tailwindcss/vite"
    - "vitest 4.1.10 + @vitest/coverage-v8 + @testing-library/react + @testing-library/jest-dom + jsdom"
    - "i18next 26.3.6 + react-i18next 17.0.11"
    - "@tauri-apps/plugin-fs 2.5.1, @tauri-apps/plugin-dialog 2.7.2 (Rust crates + npm bindings)"
    - "@fontsource-variable/inter, @fontsource-variable/jetbrains-mono (self-hosted fonts)"
    - "zustand, immer, gray-matter, unified/remark-parse/remark-gfm/react-markdown, date-fns, class-variance-authority, lucide-react (installed now, consumed starting Plan 02+)"
  patterns:
    - "One locale file per i18next namespace (src/locales/{lang}/common.json now; project.json/board.json/sync.json/artifact.json in later plans) to avoid parallel-plan file contention"
    - "Design tokens as CSS custom properties swapped via @media (prefers-color-scheme), not a manual theme toggle"
    - "Tauri capability allowlist is a closed set — every future plan must extend, never silently broaden, the 6 read/event identifiers"

key-files:
  created:
    - CONTRIBUTING.md
    - package.json / package-lock.json
    - tsconfig.json / tsconfig.node.json
    - vite.config.ts / vitest.config.ts
    - index.html
    - src/main.tsx / src/App.tsx
    - src/i18n.ts
    - src/locales/pt-BR/common.json / src/locales/en/common.json
    - src/styles/theme.css / src/styles/index.css
    - src/smoke.test.ts / src/test/setup.ts
    - src/vite-env.d.ts
    - src-tauri/Cargo.toml / Cargo.lock / build.rs / src/main.rs / src/lib.rs
    - src-tauri/tauri.conf.json
    - src-tauri/capabilities/default.json
    - src-tauri/icons/* (icon.ico, icon.icns, icon.png, 32x32.png, 64x64.png, 128x128.png, 128x128@2x.png)
    - .github/workflows/ci.yml
    - .gitignore
  modified: []

key-decisions:
  - "Task 2 package legitimacy gate: user responded 'aprovado' — all 10 packages outside the RESEARCH.md audit table approved as-is, no substitutions"
  - "Capability permission identifiers (fs:allow-read-text-file, fs:allow-read-dir, fs:allow-exists, dialog:allow-open) verified directly against the installed crate versions' autogenerated permissions/ TOML files rather than assumed from docs — exact match, no deviation needed"
  - "src-tauri/gen/schemas/ added to .gitignore — regenerated by tauri-build on every build, not meant to be committed"
  - "tauri.conf.json bundle.icon key omitted (uses Tauri's default icon path resolution); icons/ directory populated because tauri-build unconditionally requires icons/icon.ico on Windows to embed the exe resource, independent of bundle.icon config"

patterns-established:
  - "No UI string may be a literal in a component — always an i18next key (`namespace.section.key`)"
  - "CSS custom properties for design tokens, declared once in theme.css, consumed via var(--token-name) rather than duplicated Tailwind config"

requirements-completed: [PROJ-02]

coverage:
  - id: D1
    description: "Rust/MSVC toolchain unlocked and documented in CONTRIBUTING.md for Windows/macOS/Linux"
    requirement: "PROJ-02"
    verification:
      - kind: other
        ref: "cargo --version && rustc --version && rustup show (exit 0, stable-x86_64-pc-windows-msvc default)"
        status: pass
      - kind: other
        ref: "grep '## Pré-requisitos' CONTRIBUTING.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "Package legitimacy gate for the 10 packages outside the RESEARCH.md audit resolved by explicit human approval"
    verification: []
    human_judgment: true
    rationale: "Package legitimacy approval is a policy decision requiring human sign-off, not an automatable code-correctness check. Already resolved via the user's 'aprovado' response during execution — recorded here for audit trail, not re-prompted."
  - id: D3
    description: "Tauri v2 + React 19 + TypeScript app scaffold compiles, tests pass, and the capability allowlist is exactly read-only"
    requirement: "PROJ-02"
    verification:
      - kind: other
        ref: "npm run typecheck (tsc --noEmit, exit 0)"
        status: pass
      - kind: unit
        ref: "src/smoke.test.ts — both tests pass (vitest run)"
        status: pass
      - kind: other
        ref: "cargo check --manifest-path src-tauri/Cargo.toml (exit 0)"
        status: pass
      - kind: other
        ref: "node -e assertion that src-tauri/capabilities/default.json permissions === [core:default, core:event:default, dialog:allow-open, fs:allow-read-text-file, fs:allow-read-dir, fs:allow-exists]"
        status: pass
      - kind: manual_procedural
        ref: "npm run tauri dev — cargo build finished, gsd-cards.exe process observed running (tasklist), no linker errors in build output, process cleanly terminated afterward"
        status: pass
    human_judgment: false
  - id: D4
    description: "UI-SPEC design tokens implemented as Tailwind 4 @theme tokens, i18next bootstrapped with pt-BR/en key parity, CI matrix running typecheck/test/cargo check"
    requirement: "PROJ-02"
    verification:
      - kind: other
        ref: "grep checks for all 12 hex color tokens + prefers-color-scheme + @theme in src/styles/theme.css"
        status: pass
      - kind: other
        ref: "node -e key-parity check between src/locales/pt-BR/common.json and src/locales/en/common.json"
        status: pass
      - kind: other
        ref: "grep checks for windows-latest/macos-latest/ubuntu-latest/npm run test in .github/workflows/ci.yml"
        status: pass
      - kind: manual_procedural
        ref: "npm run tauri dev (second run, post i18n/theme wiring) — vite optimized i18next/react-i18next deps, cargo build finished in 6s (incremental), gsd-cards.exe process observed running, cleanly terminated"
        status: pass
    human_judgment: false

duration: 16min
completed: 2026-07-22
status: complete
---

# Phase 01 Plan 01: Espelho Fiel Foundation Summary

**Tauri v2 + React 19 + TypeScript 7 desktop app scaffold with a read-only 6-permission capability allowlist, UI-SPEC design tokens, pt-BR/en i18next bootstrap, and a 3-OS CI matrix.**

## Performance

- **Duration:** 16 min (across this continuation; Task 1 toolchain install was completed in a prior session segment)
- **Started:** 2026-07-22T22:39:37-03:00 (Task 1 commit)
- **Completed:** 2026-07-22T22:55:33-03:00 (Task 4 commit)
- **Tasks:** 4 (all complete)
- **Files modified:** 34 created (0 pre-existing files modified besides main.tsx/App.tsx which were created and then extended within this same plan)

## Accomplishments

- Rust stable (MSVC) + Visual Studio Build Tools 2022 (VCTools) installed and verified end-to-end with a real `cargo build`; documented for all three OSes in `CONTRIBUTING.md`
- Package legitimacy gate for the 10 packages outside the research audit resolved by explicit developer approval — installed exactly as presented, no substitutions
- `npm run tauri dev` opens a real native window (verified twice: once after the bare scaffold, once after design tokens/i18n were wired in) with no linker errors
- Capability allowlist (`src-tauri/capabilities/default.json`) contains exactly the 6 agreed read/event identifiers — verified byte-for-byte against the actual installed `tauri-plugin-fs`/`tauri-plugin-dialog` autogenerated permission TOML files, not just assumed from documentation
- Design tokens (spacing scale, 4-role typography, light/dark color pairs) implemented as a Tailwind 4 `@theme` block matching `01-UI-SPEC.md` exactly
- i18next bootstrapped pt-BR-first with `common` namespace key parity between `pt-BR`/`en`; `App.tsx` has zero hardcoded UI strings
- CI workflow covers `windows-latest`/`macos-latest`/`ubuntu-latest` with `npm ci`, typecheck, vitest, and `cargo check`, with Rust/cargo caching

## Task Commits

Each task was committed atomically:

1. **Task 1: Desbloquear o toolchain Rust + MSVC e documentar pré-requisitos** - `83bdb58` (docs) — completed in a prior session segment before the Task 2 checkpoint
2. **Task 2: Gate de legitimidade dos pacotes fora da auditoria da pesquisa** - no separate commit (plan specifies "nenhum arquivo é modificado" for this task); approval recorded here in this SUMMARY as the audit artifact
3. **Task 3: Scaffold do app Tauri v2 + React + TypeScript com capabilities somente-leitura** - `dbcccef` (feat)
4. **Task 4: Design tokens do UI-SPEC, bootstrap i18next e CI multiplataforma** - `a045fe0` (feat)

**Plan metadata:** _pending — created after this SUMMARY, in the final commit step_

## Files Created/Modified

- `CONTRIBUTING.md` - Toolchain prerequisites for Windows/macOS/Linux (Task 1, prior commit)
- `package.json` / `package-lock.json` - npm scripts (dev/build/tauri/test/test:planning/typecheck/lint), pinned current registry versions for the full locked stack
- `tsconfig.json` / `tsconfig.node.json` - strict TS7 config for Vite + React
- `vite.config.ts` - `@vitejs/plugin-react` + `@tailwindcss/vite`, fixed port 1420 for Tauri dev
- `vitest.config.ts` - jsdom environment, globals, setup file
- `index.html` / `src/main.tsx` / `src/App.tsx` - React entrypoint; App.tsx renders `t('app.title')`, no literal strings
- `src/i18n.ts` - i18next + react-i18next bootstrap, pt-BR default/fallback
- `src/locales/pt-BR/common.json` / `src/locales/en/common.json` - `common` namespace, key parity enforced
- `src/styles/theme.css` - `@theme` tokens (spacing, typography, light/dark colors)
- `src/styles/index.css` - Tailwind + theme + font imports
- `src/smoke.test.ts` / `src/test/setup.ts` - vitest smoke test + jest-dom setup
- `src/vite-env.d.ts` - Vite client types (needed for CSS side-effect imports to typecheck)
- `src-tauri/Cargo.toml` / `Cargo.lock` / `build.rs` / `src/main.rs` / `src/lib.rs` - minimal Tauri backend, no PTY surface (D-02)
- `src-tauri/tauri.conf.json` - window config, CSP `default-src 'self'`, identifier `dev.gsdcards.app`
- `src-tauri/capabilities/default.json` - exactly 6 read/event permission identifiers
- `src-tauri/icons/*` - generated icon set (Windows requires `icon.ico` unconditionally for the exe resource)
- `.github/workflows/ci.yml` - 3-OS CI matrix
- `.gitignore` - node_modules/dist/target/gen-schemas excluded; lockfiles NOT excluded

## Decisions Made

- Task 2 gate resolved: user approved all 10 packages ("aprovado") exactly as presented — no substitutions, no rejections
- Verified the exact capability permission identifier strings (`fs:allow-read-text-file`, `fs:allow-read-dir`, `fs:allow-exists`, `dialog:allow-open`) against the actually-installed plugin crates' autogenerated `permissions/autogenerated/commands/*.toml` files rather than trusting docs alone — confirmed an exact match, so no correction was needed
- Omitted `bundle.icon` from `tauri.conf.json` (Tauri resolves default icon paths from `icons/`) while still populating the `icons/` directory, because `tauri-build`'s build script unconditionally requires `icons/icon.ico` on Windows regardless of the `bundle.icon` config value

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added @types/react, @types/react-dom, @types/node devDependencies**
- **Found during:** Task 3 (`npm run typecheck` verification)
- **Issue:** `tsc --noEmit` failed with `TS7016: Could not find a declaration file for module 'react-dom/client'` — the plan's dependency list didn't include the `@types/*` packages needed for a strict TS config
- **Fix:** Added `@types/react@^19.2.17`, `@types/react-dom@^19.2.3`, `@types/node@^26.1.1` to `devDependencies` and re-ran `npm install`
- **Files modified:** package.json, package-lock.json
- **Verification:** `npm run typecheck` exits 0
- **Committed in:** dbcccef (Task 3 commit)

**2. [Rule 3 - Blocking] Created src/test/setup.ts**
- **Found during:** Task 3 (vitest.config.ts authoring)
- **Issue:** `vitest.config.ts`'s `setupFiles` needs a concrete file that imports `@testing-library/jest-dom`; the plan's file list didn't enumerate it separately
- **Fix:** Created `src/test/setup.ts` with `import "@testing-library/jest-dom/vitest"`
- **Files modified:** src/test/setup.ts
- **Verification:** `npm run test` passes with jest-dom matchers available
- **Committed in:** dbcccef (Task 3 commit)

**3. [Rule 3 - Blocking] Generated src-tauri/icons/* and added src-tauri/gen/schemas/ to .gitignore**
- **Found during:** Task 3 (`cargo check` verification)
- **Issue:** `cargo check` failed — `tauri-build`'s build script requires `icons/icon.ico` unconditionally on Windows ("`icons/icon.ico` not found; required for generating a Windows Resource file during tauri-build"), which the plan's file list didn't include
- **Fix:** Generated a minimal solid-color (UI-SPEC accent `#6366f1`) source PNG via a small Node script (raw PNG bytes + zlib deflate, no external image tooling needed), ran `npx tauri icon` to produce the full desktop icon set, pruned the unused Android/iOS/Appx variants, and kept only `icon.ico`, `icon.icns`, `icon.png`, `32x32.png`, `64x64.png`, `128x128.png`, `128x128@2x.png`. Also added `src-tauri/gen/schemas/` to `.gitignore` since it's regenerated by `tauri-build` on every build.
- **Files modified:** src-tauri/icons/* (new), .gitignore
- **Verification:** `cargo check --manifest-path src-tauri/Cargo.toml` exits 0; `npm run tauri dev` opens a native window with no linker errors
- **Committed in:** dbcccef (Task 3 commit)

**4. [Rule 3 - Blocking] Created src/vite-env.d.ts**
- **Found during:** Task 4 (`npm run typecheck` verification after wiring `import "./styles/index.css"` into main.tsx)
- **Issue:** `tsc --noEmit` failed with `TS2882: Cannot find module or type declarations for side-effect import of './styles/index.css'` — needs Vite's client type declarations
- **Fix:** Added `src/vite-env.d.ts` with `/// <reference types="vite/client" />`
- **Files modified:** src/vite-env.d.ts
- **Verification:** `npm run typecheck` exits 0
- **Committed in:** a045fe0 (Task 4 commit)

---

**Total deviations:** 4 auto-fixed (all Rule 3 - blocking issues)
**Impact on plan:** All four were necessary to satisfy the plan's own acceptance criteria (`npm run typecheck`/`npm run test`/`cargo check` all exiting 0). No scope creep — no new features, no architectural changes.

## Issues Encountered

None beyond the four blocking-issue auto-fixes documented above.

## User Setup Required

None - no external service configuration required. Node/npm/Rust/MSVC toolchain setup is documented in `CONTRIBUTING.md` (Task 1).

## Next Phase Readiness

- The repository now has a running, testable, CI-covered Tauri v2 + React + TypeScript app — every subsequent plan in this phase (parser, board, sessions/terminal in later phases) builds on this foundation without further scaffold work
- `src/planning/` (parser structure recommended by `01-RESEARCH.md`) does not exist yet — that's the next plan's responsibility, not a gap in this one
- No blockers for Plan 02

---
*Phase: 01-espelho-fiel*
*Completed: 2026-07-22*
