---
phase: 01-espelho-fiel
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src-tauri/src/project.rs
  - src-tauri/src/lib.rs
  - src-tauri/Cargo.toml
  - src/planning/paths.ts
  - src/planning/read.ts
  - src/planning/read.test.ts
  - src/planning/parse-result.ts
  - src/planning/model.ts
  - src/planning/parser/state.ts
  - src/planning/parser/state.test.ts
  - src/planning/__fixtures__/state/healthy-STATE.md
  - src/planning/__fixtures__/state/broken-frontmatter-STATE.md
  - src/stores/board-store.ts
  - src/shell/AppShell.tsx
  - src/shell/Header.tsx
  - src/shell/SidebarPlaceholder.tsx
  - src/shell/DrawerRail.tsx
  - src/shell/AppShell.test.tsx
  - src/components/EmptyState.tsx
  - src/components/ErrorState.tsx
  - src/components/ProgressBar.tsx
  - src/locales/pt-BR/project.json
  - src/locales/en/project.json
  - src/App.tsx
autonomous: true
requirements: [PROJ-02, BOARD-04]
must_haves:
  truths:
    - "Usuário clica em 'Abrir projeto', escolhe uma pasta com .planning/ e o app aceita o diretório como projeto GSD"
    - "Usuário escolhe uma pasta sem .planning/ e vê a tela de erro 'Esta pasta não é um projeto GSD' em vez de um board vazio"
    - "Com um projeto aberto, o header mostra nome do projeto, milestone ativo, fase atual e a barra de progresso geral lidos de STATE.md"
    - "Um STATE.md com frontmatter corrompido não derruba a tela — o app mostra o que conseguiu ler e sinaliza o que não reconheceu"
  artifacts:
    - path: "src-tauri/src/project.rs"
      provides: "Comando Tauri validate_project_root com canonicalização de caminho e concessão de escopo fs"
      contains: "canonicalize"
    - path: "src/planning/parse-result.ts"
      provides: "Tipo ParseResult com estado explícito unrecognized (base do BOARD-05)"
      exports: ["ParseResult", "ok", "unrecognized"]
    - path: "src/planning/parser/state.ts"
      provides: "Parser de STATE.md — frontmatter de progresso + blockers por fase"
      exports: ["parseStateFile"]
    - path: "src/shell/Header.tsx"
      provides: "Header fixo D-12 com nome, milestone, fase atual e progresso geral"
    - path: "src/shell/AppShell.tsx"
      provides: "Esqueleto de layout D-04 (header + sidebar 240px + área do board + drawer rail 48px)"
    - path: "src/stores/board-store.ts"
      provides: "Estado zustand do projeto aberto"
      exports: ["useBoardStore"]
  key_links:
    - from: "src/planning/read.ts"
      to: "src-tauri/src/project.rs"
      via: "invoke do comando validate_project_root antes de qualquer leitura de arquivo"
      pattern: "invoke.*validate_project_root"
    - from: "src/shell/Header.tsx"
      to: "src/stores/board-store.ts"
      via: "seletor do zustand para progresso e milestone"
      pattern: "useBoardStore"
    - from: "src/stores/board-store.ts"
      to: "src/planning/parser/state.ts"
      via: "openProject chama parseStateFile no conteúdo lido"
      pattern: "parseStateFile"
---

<objective>
Entregar a primeira fatia vertical realmente útil do produto: o usuário abre uma pasta, o app **valida** que ela é um projeto GSD (PROJ-02), lê `.planning/STATE.md` do disco e mostra no header o nome do projeto, o milestone ativo, a fase atual e a barra de progresso geral (BOARD-04, nível projeto). Pasta inválida produz uma tela de erro explícita — o app nunca finge que uma pasta qualquer é um projeto.

Junto vem o esqueleto de layout completo (D-04): header fixo, sidebar de 240px com placeholder de sessões, área central do board e drawer rail de 48px recolhido — os encaixes que as Fases 2-3 vão preencher sem retrabalho.

Este plano também estabelece dois contratos que todos os planos seguintes consomem: `ParseResult` (o tipo com estado explícito "não reconhecido", base da degradação graciosa do BOARD-05) e o comando Rust `validate_project_root`, que é o único ponto onde um caminho de filesystem entra no app.

Purpose: prova que o loop completo funciona — interação de UI → IPC → filesystem real → parse → estado → tela.
Output: app que abre projetos GSD de verdade e mostra dados reais deles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-espelho-fiel/01-CONTEXT.md
@.planning/phases/01-espelho-fiel/01-RESEARCH.md
@.planning/phases/01-espelho-fiel/01-UI-SPEC.md
@.planning/phases/01-espelho-fiel/SKELETON.md
@.planning/phases/01-espelho-fiel/01-01-SUMMARY.md
</context>

## Phase Goal

**As a** desenvolvedor que usa o fluxo GSD no dia a dia, **I want to** abrir uma pasta de projeto e ver um board kanban hierárquico que espelha o `.planning/` em tempo real, **so that** eu saiba com confiança, sem abrir o editor, exatamente onde cada fase do meu projeto está.

> Fatia vertical deste plano: o usuário **abre um projeto** e vê dados reais dele. Antes deste plano o app abria uma janela com um título estático.

## Artifacts this phase produces

Símbolos novos criados por **este plano**:

| Tipo | Símbolo / caminho |
|---|---|
| Comando Tauri (Rust) | `validate_project_root` em `src-tauri/src/project.rs`; struct `ValidatedProject { root, planning_dir, has_roadmap, has_state }`; enum de erro `ProjectError` |
| Módulo TS | `src/planning/paths.ts` → `planningDir`, `roadmapPath`, `statePath`, `phasesDir`, `milestonesDir`, `milestonesIndexPath` |
| Módulo TS | `src/planning/read.ts` → `validateProjectRoot`, `readPlanningText`, `listPlanningDir`, `planningFileExists` |
| Tipo TS | `src/planning/parse-result.ts` → `ParseResult<T>`, `ok()`, `unrecognized()`, `isUnrecognized()`, `ParseIssue` |
| Tipo TS | `src/planning/model.ts` → `ProjectStateModel`, `ProjectProgress`, `PhaseBlocker`, `MilestoneRef` |
| Parser | `src/planning/parser/state.ts` → `parseStateFile` |
| Store | `src/stores/board-store.ts` → `useBoardStore`, ações `openProject`, `closeProject`, `setProjectError` |
| Componentes React | `AppShell`, `Header`, `SidebarPlaceholder`, `DrawerRail`, `EmptyState`, `ErrorState`, `ProgressBar` |
| Locales | namespace `project` (`src/locales/{pt-BR,en}/project.json`) |
| Fixtures | `src/planning/__fixtures__/state/healthy-STATE.md`, `.../broken-frontmatter-STATE.md` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Portão de entrada seguro do filesystem — comando Rust validate_project_root + camada de leitura TS</name>
  <read_first>
    - `src-tauri/src/lib.rs`, `src-tauri/Cargo.toml`, `src-tauri/capabilities/default.json` — criados no Plano 01; a allowlist de capabilities é o contrato que esta task consome
    - `.planning/phases/01-espelho-fiel/01-RESEARCH.md` — `## Security Domain` (path traversal, escopo do plugin-fs), `## Architectural Responsibility Map` (linha "Validar `.planning/` no diretório escolhido"), `## Code Examples` → "Leitura de arquivo escopada via plugin-fs", e `## Architecture Patterns` → Pattern 4
    - `.planning/phases/01-espelho-fiel/01-CONTEXT.md` — PROJ-02 no `<domain>`; board read-only
  </read_first>
  <files>src-tauri/src/project.rs, src-tauri/src/lib.rs, src-tauri/Cargo.toml, src/planning/paths.ts, src/planning/read.ts, src/planning/read.test.ts</files>
  <behavior>
    - Diretório com `.planning/ROADMAP.md` e `.planning/STATE.md` → validação retorna sucesso com o caminho canônico
    - Diretório sem `.planning/` → validação retorna erro identificável como "não é projeto GSD" (nunca sucesso com board vazio)
    - Diretório cujo `.planning` é um symlink apontando para fora da raiz escolhida → validação retorna erro de escopo
    - Depois de uma validação bem-sucedida, uma leitura de `.planning/ROADMAP.md` funciona; uma leitura de um caminho fora da raiz validada falha
    - `planningDir(root)` e derivados produzem caminhos com separador normalizado, funcionando com raiz Windows (`C:\dev\x`) e POSIX (`/home/x`)
  </behavior>
  <action>
    **Rust — `src-tauri/src/project.rs`:** implementar o comando `#[tauri::command] validate_project_root(app, root: String) -> Result<ValidatedProject, ProjectError>`. Passos, nesta ordem:

    1. Canonicalizar a raiz recebida (resolvendo symlinks) — este é o controle que mitiga T-01-01. Nunca operar sobre o caminho cru vindo do frontend.
    2. Montar `planning_dir = root_canônico + "/.planning"` e canonicalizá-lo também. Se o `planning_dir` canônico não começar pelo `root` canônico, retornar erro de escopo (`ProjectError::OutsideScope`) — isso captura o caso do symlink que escapa da raiz.
    3. Se o `.planning` não existir, retornar `ProjectError::NotAGsdProject`.
    4. Espelhar a mesma checagem que `cmdInitManager` do gsd-core faz: verificar a existência de `.planning/ROADMAP.md` e `.planning/STATE.md` e devolvê-las como os campos booleanos `has_roadmap` e `has_state`. A ausência de um deles **não** invalida o projeto (um projeto pode estar em bootstrap); apenas o `.planning/` ausente invalida.
    5. Conceder escopo de leitura em runtime para a raiz canônica usando a extensão de escopo do `tauri-plugin-fs` (`allow_directory` recursivo sobre o root canônico). Nenhum outro diretório recebe escopo; o escopo é concedido por projeto aberto, não globalmente.
    6. Retornar `ValidatedProject { root, planning_dir, has_roadmap, has_state }` com os caminhos já canônicos.

    Registrar o comando no `invoke_handler` em `src-tauri/src/lib.rs` e registrar os plugins `tauri_plugin_fs` e `tauri_plugin_dialog` no builder. Adicionar testes `#[cfg(test)]` em `project.rs` para a lógica de contenção de caminho (a função pura que decide se um caminho canônico está contido em outro), cobrindo caso contido, caso irmão com prefixo textual comum (ex.: `/a/bc` não está contido em `/a/b`) e caso fora.

    **TypeScript — `src/planning/paths.ts`:** funções puras que derivam os caminhos canônicos do gsd-core a partir de uma raiz de projeto: `planningDir` (`<root>/.planning`), `roadmapPath` (`.../ROADMAP.md`), `statePath` (`.../STATE.md`), `phasesDir` (`.../phases`), `milestonesDir` (`.../milestones`), `milestonesIndexPath` (`.../MILESTONES.md`). Atenção: o diretório de arquivamento de milestones é `milestones`, **não** `archive` — a `01-RESEARCH.md` (Pitfall 3) corrigiu por leitura do código-fonte do gsd-core a suposição registrada em D-08; `archive` é caminho morto e nunca é escrito por nenhum workflow. Normalizar separadores para `/` internamente.

    **TypeScript — `src/planning/read.ts`:** `validateProjectRoot(root)` faz `invoke('validate_project_root', { root })` e converte o erro Rust em um erro tipado do domínio (`NotAGsdProject` | `OutsideScope` | `IoError`); `readPlanningText(path)`, `listPlanningDir(path)` e `planningFileExists(path)` encapsulam `readTextFile`, `readDir` e `exists` do `@tauri-apps/plugin-fs`. `readPlanningText` deve recusar arquivos acima de 2 MB (retornando um erro tipado `TooLarge`) — artefatos `.planning/` reais são pequenos, e o teto evita que um arquivo gigante trave o webview (T-01-05).

    **Testes — `src/planning/read.test.ts`:** cobrir `paths.ts` inteiro (funções puras, sem mock) e o mapeamento de erro de `validateProjectRoot` mockando `@tauri-apps/api/core` com `vi.mock`.
  </action>
  <acceptance_criteria>
    - `src-tauri/src/project.rs` contains `validate_project_root`
    - `src-tauri/src/project.rs` contains `canonicalize`
    - `src-tauri/src/lib.rs` contains `validate_project_root` (comando registrado no invoke_handler)
    - `cargo test --manifest-path src-tauri/Cargo.toml` exits 0 e inclui o teste de contenção de caminho onde `/a/bc` NÃO é considerado contido em `/a/b`
    - `src/planning/paths.ts` contains `export function milestonesDir`
    - Nenhum arquivo de `src/planning/` contém a substring `.planning/archive`
    - `src/planning/read.ts` contains `validate_project_root`
    - `npx vitest run src/planning/read.test.ts` exits 0 com pelo menos 6 testes passando
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/planning/read.test.ts &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Todo caminho de filesystem entra no app por um único portão que canonicaliza, valida contenção e concede escopo mínimo; os testes provam contenção correta inclusive no caso de prefixo textual enganoso.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelo do projeto e parser de STATE.md com fallback explícito</name>
  <read_first>
    - `src/planning/paths.ts`, `src/planning/read.ts` — criados na Task 1
    - `.planning/STATE.md` — artefato real deste próprio repositório, fonte do fixture saudável
    - `.planning/phases/01-espelho-fiel/01-RESEARCH.md` — `## Architecture Patterns` → Pattern 2 (linha do `STATE.md`) e Pattern 1 → "Campos de progresso (BOARD-04)"; `## Common Pitfalls` → Pitfall 2
    - `.planning/phases/01-espelho-fiel/01-CONTEXT.md` — D-09 (badge de bloqueio vindo de STATE.md), D-12 (header), D-15 (falha localizada)
  </read_first>
  <files>src/planning/parse-result.ts, src/planning/model.ts, src/planning/parser/state.ts, src/planning/parser/state.test.ts, src/planning/__fixtures__/state/healthy-STATE.md, src/planning/__fixtures__/state/broken-frontmatter-STATE.md</files>
  <behavior>
    - Fixture saudável → `parseStateFile` devolve `milestone: "v1.0"`, `currentPhase: 1`, `currentPhaseName: "Espelho fiel"`, `progress.percent: 0`, `progress.totalPhases: 5`
    - Fixture saudável → os três blockers de `### Blockers/Concerns` viram entradas com as fases corretas: `[Phase 1]` → fase 1; `[Phase 1/4]` → fases 1 **e** 4; `[Phase 2]` → fase 2
    - Fixture com frontmatter YAML truncado → resultado é `unrecognized` com uma `ParseIssue` que carrega o motivo legível, e **não** lança exceção
    - Frontmatter presente mas sem a chave `progress` → progresso vem como `unrecognized`, e os campos que existem (milestone, fase atual) continuam disponíveis; nenhum valor default enganoso (nunca `percent: 0` inventado quando o campo está ausente)
    - `progress.percent` é devolvido exatamente como está no arquivo, sem recálculo a partir de outros campos
  </behavior>
  <action>
    **`src/planning/parse-result.ts`:** definir `type ParseResult<T> = { kind: 'ok'; value: T } | { kind: 'unrecognized'; issues: ParseIssue[]; raw?: string }` e os construtores `ok(value)`, `unrecognized(issues, raw?)`, mais o predicado `isUnrecognized(result)`. `ParseIssue` carrega `{ path: string; field?: string; reason: string }` — `reason` é a mensagem legível que a UI mostra no modo raw (D-15) e no tooltip do card. Regra de projeto codificada aqui e obrigatória em todos os parsers dos planos seguintes: **nunca inferir um valor default quando a extração falha** — o resultado é `unrecognized`, porque um valor plausível e errado é exatamente o modo de falha que o Pitfall 2 descreve (board que mente em silêncio).

    **`src/planning/model.ts`:** definir `ProjectStateModel` como a raiz do estado espelhado: `{ root: string; projectName: string; milestone: ParseResult<string>; currentPhase: ParseResult<number>; currentPhaseName: ParseResult<string>; progress: ParseResult<ProjectProgress>; blockers: PhaseBlocker[]; phases: PhaseModel[]; milestones: MilestoneRef[]; issues: ParseIssue[] }`. `ProjectProgress` espelha literalmente o frontmatter do gsd-core: `totalPhases`, `completedPhases`, `totalPlans`, `completedPlans`, `percent`. `PhaseBlocker` é `{ phases: number[]; text: string }`. `PhaseModel` e `MilestoneRef` podem ficar como interfaces mínimas aqui (id, número, nome) — os Planos 03 e 06 as preenchem.

    **`src/planning/parser/state.ts`:** `parseStateFile(rawText, sourcePath): ParseResult<StateFileModel>`. Usar `gray-matter` para o frontmatter (manter o schema YAML seguro padrão — não trocar por schema permissivo nem habilitar engine alternativa; ver T-01-04). Campos extraídos do frontmatter: `milestone`, `milestone_name`, `current_phase`, `current_phase_name`, `status`, `last_updated`, e o objeto `progress` com `total_phases`, `completed_phases`, `total_plans`, `completed_plans`, `percent` — convertidos para camelCase no modelo. `percent` é lido direto, **sem recálculo** (o gsd-core já mantém esse valor; recalcular é reimplementar divergentemente).

    Do corpo, extrair os bloqueios: localizar a seção `## Accumulated Context`, dentro dela o heading `### Blockers/Concerns`, e coletar os itens de lista até o próximo heading. Cada item tem a forma `- [Phase N]: texto` ou `- [Phase N/M]: texto` (ambas ocorrem no `STATE.md` real deste repositório) — extrair todos os números de fase citados no rótulo, aceitando decimais (`2.1`). Itens que não casem com o rótulo entram como blocker sem fase associada (`phases: []`), nunca são descartados em silêncio. A linha de comentário entre colchetes que o template do gsd-core coloca sob o heading (o texto explicativo entre colchetes que não é um blocker real) e o marcador de lista vazia devem ser ignorados.

    Percorrer o corpo com `unified` + `remark-parse` para localizar headings e listas em vez de casar headings por expressão regular sobre o texto cru — o formato do gsd-core evolui na branch `next` e a AST é resiliente a variações de espaçamento e nível de heading.

    **Fixtures:** `healthy-STATE.md` é uma cópia fiel do `.planning/STATE.md` deste repositório no momento da execução (incluindo os três blockers com os rótulos `[Phase 1]`, `[Phase 1/4]`, `[Phase 2]`). `broken-frontmatter-STATE.md` é a mesma cópia com o delimitador de fechamento do frontmatter removido, para provar a degradação graciosa.
  </action>
  <acceptance_criteria>
    - `src/planning/parse-result.ts` contains `export function unrecognized`
    - `src/planning/parser/state.ts` contains `export function parseStateFile`
    - `src/planning/parser/state.ts` contains `remark-parse`
    - `npx vitest run src/planning/parser/state.test.ts` exits 0 com pelo menos 8 testes passando
    - Existe um teste cujo nome cita o rótulo de fase múltipla e que asserta que o blocker rotulado com duas fases produz `phases: [1, 4]`
    - Existe um teste que carrega `broken-frontmatter-STATE.md`, asserta `kind === 'unrecognized'` e asserta que a chamada não lançou exceção
    - `src/planning/parser/state.ts` não contém nenhuma atribuição de valor numérico literal para `percent` fora da leitura do frontmatter (o valor nunca é inventado nem recalculado)
    - `src/planning/__fixtures__/state/healthy-STATE.md` contains `gsd_state_version`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/planning/parser/state.test.ts</automated>
  </verify>
  <done>O parser de STATE.md extrai progresso e bloqueios de um artefato real e degrada para `unrecognized` — nunca para um valor default plausível — diante de um arquivo corrompido.</done>
</task>

<task type="auto">
  <name>Task 3: Esqueleto de layout, header com dados reais e fluxo "Abrir projeto"</name>
  <read_first>
    - `src/planning/read.ts`, `src/planning/paths.ts`, `src/planning/parser/state.ts`, `src/planning/model.ts` — criados nas Tasks 1 e 2
    - `src/App.tsx`, `src/i18n.ts`, `src/styles/theme.css` — criados no Plano 01
    - `.planning/phases/01-espelho-fiel/01-UI-SPEC.md` — `## Layout Skeleton (D-04)`, `## Header Bar Detail (D-12)`, `## Copywriting Contract`, `## Component Inventory`
    - `.planning/phases/01-espelho-fiel/01-CONTEXT.md` — D-04 (esqueleto completo já na Fase 1), D-12 (header fixo)
  </read_first>
  <files>src/stores/board-store.ts, src/shell/AppShell.tsx, src/shell/Header.tsx, src/shell/SidebarPlaceholder.tsx, src/shell/DrawerRail.tsx, src/shell/AppShell.test.tsx, src/components/EmptyState.tsx, src/components/ErrorState.tsx, src/components/ProgressBar.tsx, src/locales/pt-BR/project.json, src/locales/en/project.json, src/App.tsx</files>
  <action>
    **Store — `src/stores/board-store.ts`:** store zustand com `immer`, expondo `useBoardStore`. Estado: `{ status: 'idle' | 'opening' | 'open' | 'error'; project: ProjectStateModel | null; error: ProjectOpenError | null }`. Ações: `openProject(root)` (chama `validateProjectRoot`, depois `readPlanningText(statePath(root))`, depois `parseStateFile`, monta o `ProjectStateModel` e transiciona para `open`; em erro de validação transiciona para `error` com o tipo de erro preservado), `closeProject()`. Deixar declarados desde já — inicialmente vazios/estáticos — os campos que os planos seguintes populam: `phases: PhaseModel[]` (Plano 03), `milestones: MilestoneRef[]` (Plano 06), `recentlyUpdatedPhaseIds: string[]` e `sync: SyncState` (Plano 04). Declarar esses campos agora evita que planos paralelos disputem este arquivo depois.

    O store nunca escreve no filesystem — é espelho puro (Pattern 4 da pesquisa, e decisão de produto do PROJECT.md).

    **Layout — `src/shell/AppShell.tsx`:** composição fiel ao diagrama do UI-SPEC: `Header` de 56px em largura total no topo; abaixo, uma linha com `SidebarPlaceholder` de 240px fixos (superfície secundária), a área central do board (superfície dominante, `flex-1`, 32px de padding externo, largura confortável mínima de 1024px com scroll horizontal em vez de compressão) e o `DrawerRail` de 48px à direita (superfície secundária). Quando `status !== 'open'`, a área central mostra `EmptyState` (nenhum projeto aberto) ou `ErrorState` (pasta inválida) ocupando a área inteira — **nunca** um board com zero cards, conforme a linha "`.planning/` missing at open time (PROJ-02)" da tabela de estados do UI-SPEC.

    `SidebarPlaceholder`: uma única linha de texto Body em tom apagado com a chave de sessões futuras, sem interação. `DrawerRail`: rail de 48px com um ícone `TerminalSquare` do `lucide-react` desabilitado e tooltip apontando para a Fase 2; não é expansível nesta fase.

    **Header — `src/shell/Header.tsx` (D-12):** 56px de altura, superfície secundária, 24px de padding horizontal. Da esquerda para a direita: nome do projeto (tipografia Display), badge de milestone (pílula neutra, tipografia Label, ex.: "v1.0"), indicador de fase atual (Body, ex.: "Fase 1 de 5"), barra de progresso geral de 120px de largura com preenchimento na cor de acento, alimentada **exclusivamente** por `progress.percent` do STATE.md. Reservar, à direita, o slot onde o indicador de sincronização entra no Plano 04 e, entre a barra e esse slot, o slot dos contadores por coluna que o Plano 03 preenche — os slots existem como espaços vazios agora para que aqueles planos não precisem reestruturar o header.

    Quando qualquer campo do header vier como `unrecognized`, renderizar um traço/placeholder e o tooltip de aviso em vez de um valor inventado.

    **`src/components/ProgressBar.tsx`:** barra reutilizável — trilho de 4px na superfície secundária escurecida, preenchimento na cor de acento, largura configurável, com `role="progressbar"` e `aria-valuenow`/`aria-valuemin`/`aria-valuemax`. É a mesma barra que o card de fase usa no Plano 03.

    **`EmptyState` e `ErrorState`:** componentes genéricos com props `heading`, `body` e `action` opcional, usados aqui nas variantes "nenhum projeto aberto" (com o CTA primário) e "pasta não é projeto GSD".

    **Fluxo "Abrir projeto":** o CTA primário (cor de acento) do `EmptyState` chama `open({ directory: true, multiple: false })` do `@tauri-apps/plugin-dialog` e passa o caminho escolhido para `openProject`. Nenhum caminho é digitado ou construído no frontend — o único caminho que entra vem do diálogo nativo e passa pela validação canônica da Task 1.

    **Locales — namespace `project`:** criar `src/locales/pt-BR/project.json` e `src/locales/en/project.json` com as chaves e os textos-fonte exatos do `## Copywriting Contract` do UI-SPEC: `project.actions.open` = "Abrir projeto"; `project.empty.heading` = "Nenhum projeto aberto"; `project.empty.body` = "Abra uma pasta com um diretório .planning/ existente para começar a espelhar o progresso do seu projeto GSD."; `project.error.notGsd.heading` = "Esta pasta não é um projeto GSD"; `project.error.notGsd.body` = "Não encontramos um diretório .planning/ aqui. Escolha a pasta raiz de um projeto GSD existente."; mais `project.header.milestone`, `project.header.phaseOfTotal` (com interpolação de fase atual e total) e `project.header.progress`. Registrar o namespace `project` no `src/i18n.ts`. `en` recebe as traduções equivalentes, com paridade total de chaves.

    **Teste — `src/shell/AppShell.test.tsx`:** com Testing Library, cobrir três estados renderizados a partir de um store pré-populado: (a) `idle` mostra o heading de estado vazio e o botão do CTA; (b) `error` do tipo "não é projeto GSD" mostra o heading de erro e **não** mostra o board; (c) `open` mostra o nome do projeto, o rótulo de milestone e a barra de progresso com o `aria-valuenow` igual ao `percent` do modelo.

    Ajustar `src/App.tsx` para renderizar `AppShell`.
  </action>
  <acceptance_criteria>
    - `src/shell/AppShell.tsx` contains `SidebarPlaceholder`, `DrawerRail` e `Header`
    - `src/shell/Header.tsx` contains `aria-valuenow` ou renderiza `ProgressBar` (grep por `ProgressBar`)
    - `src/stores/board-store.ts` contains `export const useBoardStore`
    - `src/stores/board-store.ts` contains `recentlyUpdatedPhaseIds` e `milestones` (slots pré-declarados para os planos 04 e 06)
    - `src/locales/pt-BR/project.json` contains `Esta pasta não é um projeto GSD`
    - `src/locales/pt-BR/project.json` contains `Abrir projeto`
    - `src/locales/pt-BR/project.json` e `src/locales/en/project.json` têm conjuntos de chaves idênticos
    - Nenhum arquivo em `src/shell/` ou `src/components/` contém uma string literal em português entre aspas fora de comentário (todas as strings passam por `t(`) — verificável por: cada arquivo `.tsx` desses diretórios que renderiza texto contém `useTranslation`
    - `npx vitest run src/shell/AppShell.test.tsx` exits 0 com 3 testes passando
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/shell/AppShell.test.tsx &amp;&amp; npm run typecheck</automated>
    <human-check>Rodar `npm run tauri dev`. (1) A janela abre no estado vazio com o CTA "Abrir projeto". (2) Escolher `C:\dev\gsd-cards` → o header mostra "GSD Cards", o badge "v1.0", "Fase 1 de 5" e a barra de progresso. (3) Escolher qualquer pasta sem `.planning/` → aparece a tela "Esta pasta não é um projeto GSD", e não um board vazio.</human-check>
  </verify>
  <done>O usuário abre uma pasta real e vê dados reais do `.planning/` no header; pasta inválida produz erro explícito; o esqueleto de layout completo (D-04) está de pé com os encaixes das Fases 2-3 reservados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diálogo nativo → backend Rust | O caminho escolhido pelo usuário é entrada não confiável (pode ser symlink, junction, ou apontar para fora do esperado) |
| filesystem → parser TS | Conteúdo de `.planning/` pode vir de um repositório clonado de terceiros — YAML e markdown são entrada não confiável |
| backend Rust → webview (IPC) | O escopo de leitura concedido ao frontend define o alcance de qualquer bug ou injeção posterior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Information Disclosure | `validate_project_root` em `src-tauri/src/project.rs` | high | mitigate | Canonicalizar raiz e `.planning` (resolvendo symlinks) antes de qualquer uso; rejeitar quando o `.planning` canônico não estiver contido na raiz canônica; comparação de contenção por componentes de caminho, não por prefixo de string (teste obrigatório: `/a/bc` não contido em `/a/b`) |
| T-01-03b | Elevation of Privilege | concessão de escopo do `tauri-plugin-fs` | high | mitigate | Escopo concedido em runtime apenas para a raiz canônica do projeto aberto, dentro do comando de validação; nenhum escopo global nem `**` no `capabilities/default.json` |
| T-01-04 | Tampering / RCE | `gray-matter` (YAML) em `parser/state.ts` | medium | mitigate | Manter o schema YAML seguro padrão do `gray-matter`; não configurar engine YAML alternativa nem schema permissivo; frontmatter que falhe o parse vira `unrecognized`, não exceção |
| T-01-05 | Denial of Service | `readPlanningText` | low | mitigate | Teto de 2 MB por arquivo lido, retornando erro tipado `TooLarge` em vez de carregar o conteúdo no webview |
| T-01-06a | Spoofing (integridade do espelho) | `parse-result.ts` / `parser/state.ts` | high | mitigate | Contrato `ParseResult` com estado `unrecognized` explícito; proibição de valor default inferido — um progresso plausível e errado é pior que um progresso ausente, porque destrói a confiança que é o core value do produto |
</threat_model>

<verification>
- `npx vitest run src/planning` exits 0
- `cargo test --manifest-path src-tauri/Cargo.toml` exits 0
- `npm run typecheck` exits 0
- Abrir `C:\dev\gsd-cards` no app mostra nome, milestone "v1.0", "Fase 1 de 5" e a barra de progresso
- Abrir uma pasta sem `.planning/` mostra a tela de erro PROJ-02, não um board vazio
</verification>

<success_criteria>
- PROJ-02 satisfeito ponta a ponta: validação acontece antes de qualquer estado de projeto ser criado, e a falha é visível e explicada
- BOARD-04 (nível projeto) satisfeito: o progresso exibido é literalmente `STATE.md progress.percent`, sem recálculo
- O esqueleto de layout D-04 está completo, com os slots das Fases 2-3 reservados
- Nenhuma string de UI hardcoded; paridade de chaves pt-BR/en
</success_criteria>

<output>
Create `.planning/phases/01-espelho-fiel/01-02-SUMMARY.md` when done
</output>
