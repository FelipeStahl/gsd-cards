---
phase: 02-sess-o-viva
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-04, 02-05]
files_modified:
  - src/components/terminal/focus-algorithm.ts
  - src/components/terminal/focus-algorithm.test.ts
  - src/components/terminal/TerminalView.tsx
  - src/stores/session-store.ts
  - src/pty/channel.ts
  - src/components/session/SessionRow.tsx
  - src/components/session/ConfirmDialog.tsx
  - src/locales/pt-BR/session.json
  - src/locales/en/session.json
autonomous: true
requirements: [SESS-03, SESS-06]
must_haves:
  truths:
    - "Ao trocar a sessão ativa, a sessão que perde o foco tem addon-webgl disposto primeiro, o buffer serializado via addon-serialize e guardado, e a instância xterm.js disposta — o processo PTY continua vivo em background (SESS-03)"
    - "Bytes recebidos por uma sessão sem instância montada são empilhados num backgroundBuffer em memória; ao reganhar foco, a nova instância restaura o snapshot serializado e drena o backgroundBuffer em ordem, sem lacunas (SESS-03)"
    - "addon-webgl é carregado apenas na instância em foco (no máximo um contexto WebGL vivo por vez), respeitando o limite de contextos do Chromium (SESS-03)"
    - "Arquivar uma sessão mata a árvore de processos (se viva) e remove a row da lista, sem confirmação; Excluir faz o mesmo após um ConfirmDialog (SESS-06)"
    - "Nem Arquivar nem Excluir apagam o arquivo .jsonl de histórico do Claude Code — a cópia do ConfirmDialog deixa isso explícito (SESS-06, Pitfall 5)"
    - statement: "Iniciar algo de longa duração numa sessão, trocar para outra e voltar mostra o output produzido enquanto estava em background, sem lacunas"
      verification: backstop
  prohibitions:
    - "MUST NOT manter todas as instâncias xterm.js montadas escondidas via CSS — usar o algoritmo dispose+serialize (xterm.js não suporta reattach oficial; CLAUDE.md prescreve dispose+serialize)"
    - "MUST NOT apagar o arquivo .jsonl de histórico do Claude Code ao arquivar/excluir uma sessão — SESS-06 fala de processos, não do dado de outra ferramenta (Pitfall 5)"
  artifacts:
    - src/components/terminal/focus-algorithm.ts
    - src/components/terminal/focus-algorithm.test.ts
    - src/components/session/ConfirmDialog.tsx
  key_links:
    - "focus-algorithm.ts isola a lógica pura (dispose/serialize/restore/flush de buffer) do DOM real, testável com Terminal/SerializeAddon mockados"
    - "o onmessage do Channel é redirecionado: terminal.write() quando em foco, backgroundBuffer.push() quando em background — o PTY nunca para"
    - "Arquivar/Excluir chamam killSession (Plano 01) — a árvore de processos morre pelo mesmo TreeGuard do tracer"
---

<objective>
Fecha o slice da fase: múltiplas sessões vivas em paralelo com troca de foco sem fechar nenhuma (SESS-03) e as affordances de ciclo de vida arquivar/excluir com kill de árvore (SESS-06 UI). É a expansão mais complexa — o algoritmo de foco do xterm.js (dispose+serialize+restore+flush) que o `CLAUDE.md` prescreve literalmente — construída por último, sobre o terminal já provado e enriquecido.

Purpose: SESS-03 exige navegar entre sessões sem fechá-las, com os terminais inativos vivos em background; o algoritmo de foco resolve simultaneamente o limite de contextos WebGL e o custo de render de N terminais. SESS-06 ganha suas affordances de UI (arquivar/excluir), que reusam o kill de árvore do tracer sem tocar o dado de histórico do Claude Code.
Output: `focus-algorithm.ts` (+ teste), expansão de `TerminalView`/`session-store`/`channel.ts` para background, `ConfirmDialog`, ações arquivar/excluir na `SessionRow`.
</objective>

<execution_context>
@/home/user/gsd-cards/.claude/gsd-core/workflows/execute-plan.md
@/home/user/gsd-cards/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-sess-o-viva/02-UI-SPEC.md
@.planning/phases/02-sess-o-viva/02-RESEARCH.md
@.planning/phases/02-sess-o-viva/02-VALIDATION.md
@src/components/terminal/TerminalView.tsx
@src/stores/session-store.ts
@src/pty/channel.ts
@src/components/session/SessionRow.tsx
@src/components/ArtifactModal.tsx
</context>

## Artifacts this phase produces (introduzidos por este plano)

- TS: `src/components/terminal/focus-algorithm.ts` (lógica pura de lose-focus/gain-focus: dispose/serialize/snapshot/restore/flush)
- Teste: `src/components/terminal/focus-algorithm.test.ts`
- React modificado: `src/components/terminal/TerminalView.tsx` (webgl-só-no-foco, aplica o algoritmo de foco)
- TS modificado: `src/stores/session-store.ts` (`liveSessions: Map<id, LiveSessionState>` com serializedSnapshot + backgroundBuffer, troca de activeSessionId)
- TS modificado: `src/pty/channel.ts` (redireciona onmessage entre terminal.write e backgroundBuffer)
- React modificado: `src/components/session/SessionRow.tsx` (botões Archive/Trash2 no hover)
- React: `src/components/session/ConfirmDialog.tsx` (modal genérico pequeno — heading/body/confirm/cancel, tone destructive|neutral)
- i18n: expansão de `session.json` — `session.actions.archive/delete`, `session.confirmDelete.*`, `session.row.exited`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Algoritmo de foco multi-sessão (SESS-03)</name>
  <reversibility rating="reversible">O algoritmo de foco vive por trás de focus-algorithm.ts + do estado liveSessions do store; ajustá-lo é local a esses arquivos.</reversibility>
  <files>src/components/terminal/focus-algorithm.ts, src/components/terminal/focus-algorithm.test.ts, src/components/terminal/TerminalView.tsx, src/stores/session-store.ts, src/pty/channel.ts</files>
  <read_first>
    - src/components/terminal/TerminalView.tsx (ciclo de vida do Plano 01/05 — estender com webgl-no-foco + snapshot/restore)
    - src/stores/session-store.ts (activeSessionId do Plano 01, sessions[] do Plano 04 — adicionar liveSessions Map)
    - src/pty/channel.ts (onmessage do Plano 01 — redirecionar)
    - src/stores/board-store.ts linhas 650-652 (comentário sobre batching de transições para evitar flicker — mesma disciplina para bytes de alta frequência)
    - .planning/phases/02-sess-o-viva/02-RESEARCH.md Pattern 3 (algoritmo exato de perder/ganhar foco, passo a passo), Assumptions Log A4, Pitfall 4 (StrictMode)
    - .planning/phases/02-sess-o-viva/02-VALIDATION.md ## Per-Task Verification Map (SESS-03: focus-algorithm.test.ts)
  </read_first>
  <behavior>
    - Perder foco: se webgl carregado → webglAddon.dispose() primeiro; serialize() guarda snapshot no liveSessions; terminal.dispose(); onmessage passa a empilhar bytes no backgroundBuffer
    - Ganhar foco: nova instância → open(container) → carrega fit/search/web-links → write(snapshot) se existir → drena backgroundBuffer em ordem → carrega webgl agora → onmessage volta a terminal.write() → fit + resize_session
    - Um processo PTF em background continua produzindo output; ao voltar, o buffer acumulado aparece sem lacunas nem duplicação
    - No máximo um contexto WebGL vivo a qualquer momento
  </behavior>
  <action>
    Criar src/components/terminal/focus-algorithm.ts com funções puras que operam sobre um contrato mínimo (interfaces de Terminal/SerializeAddon/WebglAddon + o slice de liveSessions), implementando os passos EXATOS do Pattern 3: `loseFocus(session)` (dispose webgl → serialize → guardar snapshot → dispose terminal → redirecionar onmessage para backgroundBuffer) e `gainFocus(session, container)` (nova instância → open → addons base → write(snapshot) → drenar backgroundBuffer em ordem → limpar buffer → carregar webgl → redirecionar onmessage para write → fit + resizeSession). Expandir src/stores/session-store.ts com `liveSessions: Map<string, LiveSessionState>` (serializedSnapshot, backgroundBuffer, hasWebgl) e a transição de troca de activeSessionId que dispara lose/gain, com batching de bytes (não um set() por chunk — flush agendado). Ajustar src/pty/channel.ts para expor o redirecionamento do onmessage (write vs push). Ajustar TerminalView.tsx para delegar o ciclo de vida montar/desmontar ao focus-algorithm e carregar webgl só na instância em foco. Escrever src/components/terminal/focus-algorithm.test.ts primeiro (RED): com Terminal/SerializeAddon/WebglAddon mockados, afirmar a ordem das chamadas em loseFocus, e que gainFocus restaura snapshot + drena backgroundBuffer sem lacuna nem duplicação; depois implementar até verde.
  </action>
  <verify>
    <automated>npx vitest run src/components/terminal/focus-algorithm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/terminal/focus-algorithm.ts contém `loseFocus` e `gainFocus` (grep -q)
    - focus-algorithm.test.ts afirma dispose do webgl ANTES do serialize em loseFocus, e drenagem ordenada do backgroundBuffer em gainFocus
    - session-store expõe `liveSessions` (grep -q)
    - webgl é carregado só na instância em foco (teste afirma no máximo um hasWebgl true)
    - `npx vitest run src/components/terminal/focus-algorithm.test.ts` sai 0
  </acceptance_criteria>
  <done>Trocar de sessão dispõe/serializa a antiga e restaura/drena a nova sem lacunas; PTYs de background seguem vivos; no máximo um contexto WebGL.</done>
</task>

<task type="auto">
  <name>Task 2: Affordances de ciclo de vida — Arquivar/Excluir + ConfirmDialog (SESS-06 UI)</name>
  <files>src/components/session/SessionRow.tsx, src/components/session/ConfirmDialog.tsx, src/locales/pt-BR/session.json, src/locales/en/session.json</files>
  <read_first>
    - src/components/session/SessionRow.tsx (Plano 04 — adicionar botões de hover)
    - src/components/ArtifactModal.tsx (padrão de modal/backdrop 60% da Fase 1 — o ConfirmDialog é uma versão menor, distinta)
    - src/pty/channel.ts (killSession — reusado por arquivar/excluir)
    - .planning/phases/02-sess-o-viva/02-UI-SPEC.md ## Session Lifecycle Affordances (Archive sem confirmação; Delete com ConfirmDialog; app-close sem diálogo), ## Copywriting Contract (session.confirmDelete.*), ## Component Inventory (ConfirmDialog)
    - .planning/phases/02-sess-o-viva/02-RESEARCH.md Pitfall 5 (arquivar ≠ apagar histórico do Claude Code)
  </read_first>
  <action>
    Adicionar a src/components/session/SessionRow.tsx dois botões 32×32 que aparecem só no hover: Archive (ícone Archive, neutral) e Delete (ícone Trash2, vira `--color-destructive` no hover do próprio botão). Archive: sem confirmação, chama killSession (mata a árvore se viva) + remove a row da lista da run atual (fade+collapse 150ms); NÃO toca o .jsonl. Delete: abre o ConfirmDialog e, após confirmar, mesma ação de kill+remove. Criar src/components/session/ConfirmDialog.tsx: modal genérico pequeno (max-width 400px, centralizado, superfície dominant, backdrop 60% como o ArtifactModal), props heading/body/confirm/cancel + `tone: "destructive"|"neutral"` para a cor do botão primário; reusável por fases futuras. Adicionar as chaves session.actions.archive/delete, session.confirmDelete.heading/body/confirm/cancel, session.row.exited a session.json (pt-BR + en) — o body do confirmDelete deixa explícito que o histórico da conversa permanece salvo pelo Claude Code. O fechamento do app não mostra diálogo (RunEvent::ExitRequested do Plano 01 já mata as árvores silenciosamente). Teste colocado cobrindo: Archive chama killSession sem dialog; Delete só chama killSession após confirmar; Cancel não chama nada.
  </action>
  <verify>
    <automated>npx vitest run src/components/session/SessionRow.test.tsx src/components/session/ConfirmDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/session/ConfirmDialog.tsx existe com props heading/body/confirm/cancel e tone
    - SessionRow tem botões Archive e Delete no hover (grep -q Archive; grep -q Trash2)
    - teste afirma: Archive chama killSession sem dialog; Delete chama killSession só após confirmar; Cancel não chama
    - session.json (pt-BR e en) contém confirmDelete.body deixando claro que o histórico do Claude Code não é apagado
    - `npx vitest run src/components/session/SessionRow.test.tsx src/components/session/ConfirmDialog.test.tsx` sai 0
  </acceptance_criteria>
  <done>Arquivar/Excluir matam a árvore de processos e removem a row; Excluir exige confirmação; nenhum arquivo de histórico do Claude Code é tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI de ciclo de vida → killSession (backend) | arquivar/excluir dispara o kill de árvore no backend — a UI decide QUANDO, o backend decide COMO |
| UI → arquivo .jsonl do Claude Code | fronteira que NÃO deve ser cruzada com escrita/remoção nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Denial of Service | sessão de background sem instância acumula buffer sem limite ou vaza recursos (focus-algorithm.ts/session-store) | medium | mitigate | backgroundBuffer é drenado e limpo ao reganhar foco; webgl disposto ao perder foco (no máximo um contexto vivo); PTY é a única fonte, morto por killSession/ExitRequested |
| T-02-09 | Tampering (dado de terceiro) | arquivar/excluir apagar o .jsonl de histórico do Claude Code (SessionRow/ConfirmDialog) | medium | mitigate | arquivar/excluir só matam o processo + escondem a row; nenhuma escrita/remoção no namespace ~/.claude/projects/ (Pitfall 5); cópia do ConfirmDialog comunica isso ao usuário |
| T-02-05 | Tampering (visual) | snapshot serializado reintroduzido via write() contém escape malicioso (focus-algorithm.ts) | low | mitigate | snapshot vem do serialize() do próprio xterm.js e volta por terminal.write() — mesmo caminho seguro, nunca HTML |
| T-02-SC | Tampering | nenhuma dependência nova (addons @xterm já instalados no Plano 01) | low | accept | sem instalação neste plano |
</threat_model>

## Flagged Planner Assumptions

- **Edge-probe unclassified (SESS-03, SESS-06):** classificados `unclassified/unresolved`. Critérios de aceite concretos autorados em `must_haves.truths`; o "replay sem lacunas ao voltar de background" fica como marcador `verification: backstop` (só observável com processos reais ao longo do tempo, 02-VALIDATION.md Manual-Only). Sem descarte silencioso.
- **A4 (buffer de background):** a composição "empilhar bytes enquanto sem instância montada e drenar ao voltar" é síntese da pesquisa sobre os padrões oficiais dos addons, não um exemplo documentado único; coberta pelo focus-algorithm.test.ts + UAT manual (Assumptions Log A4).

<verification>
- `npx vitest run src/components/terminal src/components/session` verde
- UAT manual (SESS-03): abrir 2+ sessões, iniciar algo longo numa, trocar e voltar — confirmar buffer restaurado sem lacunas (02-VALIDATION.md Manual-Only)
- UAT manual (SESS-06 fundação): abrir N sessões, arquivar/excluir e fechar o app — inspecionar processos reais confirmando zero remanescentes (gate não-negociável antes de /gsd-verify-work)
</verification>

<success_criteria>
- Troca de sessão sem fechar nenhuma; terminais de background vivos com replay sem lacunas; no máximo um contexto WebGL
- Arquivar/Excluir com kill de árvore, sem tocar o histórico do Claude Code; app-close mata tudo silenciosamente
</success_criteria>

<output>
Create `.planning/phases/02-sess-o-viva/02-06-SUMMARY.md` when done
</output>
