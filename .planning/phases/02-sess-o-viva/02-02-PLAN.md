---
phase: 02-sess-o-viva
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/bin/tree_kill_helper.rs
  - src-tauri/tests/tree_kill.rs
autonomous: true
requirements: [SESS-06]
must_haves:
  truths:
    - "cargo test tree_kill_leaves_no_zombies spawna um processo que spawna um neto, aciona TreeGuard::kill_tree e afirma via sysinfo que nenhum PID da árvore original permanece vivo (SESS-06)"
    - "O binário auxiliar tree_kill_helper imprime, aguarda, e spawna um neto de vida curta — simulando o claude spawnando um subprocesso (git/MCP) (SESS-06)"
    - statement: "Numa máquina Windows real, abrir N sessões e matá-las (individualmente e via fechar o app) deixa zero processos remanescentes no Task Manager"
      verification: backstop
  prohibitions:
    - "MUST NOT confiar apenas em Child::kill() do portable-pty para SESS-06 — ele mata só o processo direto, nunca os descendentes"
  artifacts:
    - src-tauri/tests/tree_kill.rs
    - src-tauri/src/bin/tree_kill_helper.rs
  key_links:
    - "O teste de integração exercita process_guard::TreeGuard do crate lib diretamente (não via Channel), spawnando o helper como processo real"
    - "A verificação de 'zero PIDs' usa sysinfo para inspecionar a tabela de processos real do SO, não a UI"
---

<objective>
Prova de fundação de SESS-06: transformar o "encerramento limpo da árvore de processos" — o maior risco explícito desta fase — em um teste de integração automatizado que roda na matriz de CI multiplataforma (Windows/macOS/Linux) da Fase 1. O Plano 01 já implementou o mecanismo (TreeGuard) e o teardown no fechamento do app; este plano PROVA que ele não deixa zumbis, spawnando processos reais com netos e verificando a tabela de processos do SO.

Purpose: SESS-06 é critério de fundação não-negociável; a UI deixar de mostrar o terminal não é evidência de que a árvore morreu (Pitfall 1). Este é o teste de processo real que fecha essa lacuna.
Output: `tree_kill_helper.rs` (binário auxiliar com neto), `tests/tree_kill.rs` (teste de integração `tree_kill_leaves_no_zombies`).
</objective>

<execution_context>
@/home/user/gsd-cards/.claude/gsd-core/workflows/execute-plan.md
@/home/user/gsd-cards/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sess-o-viva/02-RESEARCH.md
@.planning/phases/02-sess-o-viva/02-VALIDATION.md
@src-tauri/src/process_guard.rs
@src-tauri/src/project.rs
</context>

## Artifacts this phase produces (introduzidos por este plano)

- Rust: `src-tauri/src/bin/tree_kill_helper.rs` (binário auxiliar de teste — imprime um marcador, spawna um neto de vida curta, aguarda)
- Rust: `src-tauri/tests/tree_kill.rs` (teste de integração `tree_kill_leaves_no_zombies`)
- Cargo dev-dependency novo: `sysinfo` (inspeção da tabela de processos real do SO no teste)

<tasks>

<task type="auto">
  <name>Task 1: Binário auxiliar com neto + dependência sysinfo</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/bin/tree_kill_helper.rs</files>
  <read_first>
    - src-tauri/src/process_guard.rs (assinaturas de TreeGuard::attach/kill_tree confirmadas no Plano 01)
    - src-tauri/Cargo.toml (formato de [dependencies]; adicionar [dev-dependencies] e [[bin]] se necessário)
    - .planning/phases/02-sess-o-viva/02-RESEARCH.md ## Validation Architecture "Nota sobre SESS-06 não ser automatizável em CI" (spawn → neto → kill → assert via sysinfo/tasklist/ps)
    - .planning/phases/02-sess-o-viva/02-VALIDATION.md ## Wave 0 Requirements (binário/script auxiliar) e Manual-Only
  </read_first>
  <action>
    Adicionar `sysinfo` como `[dev-dependencies]` no src-tauri/Cargo.toml (versão estável corrente; travar no Cargo.lock). Criar src-tauri/src/bin/tree_kill_helper.rs: um binário mínimo que (1) imprime uma linha-marcador determinística no stdout (para o teste saber que subiu), (2) spawna um "neto" de vida curta cross-platform via `std::process::Command` (ex.: um `sleep`/`timeout` longo) e imprime o PID do neto, (3) faz `sleep` num loop longo para se manter vivo até ser morto. O objetivo é reproduzir a topologia "claude (filho) → subprocesso (neto)" que o kill de árvore precisa alcançar. Declarar o binário em Cargo.toml (`[[bin]] name = "tree_kill_helper"`) se a convenção de src/bin não bastar para o harness de teste.
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml --bin tree_kill_helper</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --manifest-path src-tauri/Cargo.toml --bin tree_kill_helper` sai 0
    - src-tauri/src/bin/tree_kill_helper.rs contém a lógica de spawn do neto e a impressão do PID (grep -q)
    - src-tauri/Cargo.toml contém `sysinfo` em [dev-dependencies] (grep -q sysinfo)
  </acceptance_criteria>
  <done>Binário auxiliar compila e, ao rodar, spawna um neto e reporta os PIDs; sysinfo disponível para os testes.</done>
</task>

<task type="auto">
  <name>Task 2: Teste de integração tree_kill_leaves_no_zombies</name>
  <precondition>src-tauri/src/bin/tree_kill_helper compila e reporta o PID do neto (Task 1 deste plano)</precondition>
  <files>src-tauri/tests/tree_kill.rs</files>
  <read_first>
    - src-tauri/src/process_guard.rs (TreeGuard — o alvo sob teste)
    - src-tauri/tests/ (nenhum teste de integração existe ainda; seguir a convenção de src-tauri; ver planning_watcher.rs #[cfg(test)] para estilo de asserção)
    - .planning/phases/02-sess-o-viva/02-VALIDATION.md ## Per-Task Verification Map (SESS-06: cargo test tree_kill_leaves_no_zombies)
  </read_first>
  <action>
    Criar src-tauri/tests/tree_kill.rs com `tree_kill_leaves_no_zombies`: (1) spawna o binário `tree_kill_helper` via `portable_pty` (ou `std::process::Command` anexado ao PTY, conforme o que TreeGuard::attach aceita), captura o PID do processo e do neto (lendo o stdout marcador); (2) constrói `TreeGuard::attach` sobre o filho; (3) confirma que ambos os PIDs estão vivos via `sysinfo::System::refresh_processes` (assert positivo, evita teste vacuoso); (4) chama `guard.kill_tree()` (Unix) ou dropa o Job (Windows, kill-on-close), depois `child.wait()`; (5) após um pequeno grace period + refresh do sysinfo, afirma que NEM o PID do filho NEM o do neto ainda existem na tabela de processos. Estruturar as asserções para falharem com mensagem clara indicando qual PID sobreviveu. Marcar `#[cfg(...)]` onde a topologia de grupo divergir entre plataformas, mas manter o teste rodando nas três plataformas da matriz de CI.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --test tree_kill tree_kill_leaves_no_zombies</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path src-tauri/Cargo.toml --test tree_kill tree_kill_leaves_no_zombies` sai 0
    - src-tauri/tests/tree_kill.rs afirma processos VIVOS antes do kill e AUSENTES depois (grep -q sysinfo tree_kill.rs; o teste é não-vacuoso)
    - O teste referencia tanto o PID do filho quanto o do neto (grep -qi neto OR grandchild em tree_kill.rs)
  </acceptance_criteria>
  <done>Teste de integração verde nas três plataformas de CI: spawnar processo com neto e matar a árvore não deixa nenhum PID original vivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teste → SO (spawn de processos reais) | o teste cria processos reais e inspeciona a tabela de processos do SO |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Denial of Service | zumbi/órfão não coberto pelo kill de árvore (process_guard.rs) | high | mitigate | teste de integração que prova zero PIDs remanescentes após kill_tree, incluindo netos — fecha a lacuna que Child::kill() sozinho deixaria |
| T-02-SC | Tampering | dev-dependency `sysinfo` nova | medium | mitigate | crate de inspeção de processos amplamente usado; adicionado só como dev-dependency (não entra no binário de produção); travar versão no Cargo.lock |
</threat_model>

## Flagged Planner Assumptions

- **Edge-probe unclassified (SESS-06):** classificado `unclassified/unresolved`. Critério de aceite concreto autorado como `must_haves.truths` (o teste de integração); a confirmação "zero zumbis no Windows real" fica como marcador `verification: backstop` (UAT na máquina do autor, 02-VALIDATION.md Manual-Only). Sem descarte silencioso.
- **A2 (Unix session leader):** o teste valida a topologia em Linux/macOS de CI; se `portable-pty` não fizer `setsid` implícito em alguma variante Unix, `killpg` pode não alcançar o neto — o teste é justamente o detector dessa quebra (Assumptions Log A2).

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml` completo verde, incluindo o teste de integração de tree-kill
- UAT manual de SESS-06 (Windows real) permanece o gate de fundação em `/gsd-verify-work` (02-VALIDATION.md Manual-Only)
</verification>

<success_criteria>
- SESS-06 tem prova automatizada de "sem zumbis" rodando na matriz de CI multiplataforma
- O teste é não-vacuoso (afirma vivo-antes / morto-depois, para filho E neto)
</success_criteria>

<output>
Create `.planning/phases/02-sess-o-viva/02-02-SUMMARY.md` when done
</output>
