---
phase: 02-sess-o-viva
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - src/stores/session-store.ts
  - src/components/session/SessionSidebar.tsx
  - src/components/session/SessionRow.tsx
  - src/components/session/ToolMissingState.tsx
  - src/shell/AppShell.tsx
  - src/shell/DrawerRail.tsx
  - src/locales/pt-BR/session.json
  - src/locales/en/session.json
autonomous: true
requirements: [SESS-01, PROJ-04]
must_haves:
  truths:
    - "Ao abrir um projeto, a SessionSidebar lista as sessões descobertas em ~/.claude/projects/<encoded>/ em dois grupos rotulados Ativas/Histórico, cada um ordenado por lastModified descendente (SESS-01)"
    - "Zero sessões renderiza o EmptyState (session.empty.heading/body) com o CTA Nova sessão inline — nunca uma sidebar em branco (SESS-01)"
    - "Um grupo vazio não renderiza seu header (sem 'Histórico' órfão sobre nada); label da sessão trunca com ellipsis + title tooltip com o id completo (SESS-01)"
    - "Quando `claude` ou gsd-core estão ausentes, o ToolMissingState substitui o corpo da sidebar e o botão Nova sessão NÃO é renderizado (PROJ-04, Pitfall 6)"
    - "O ToolMissingState mostra a variante de cópia correta entre três: claude ausente / gsd-core ausente / ambos ausentes (PROJ-04)"
    - "A row de sessão histórica não abre o drawer ao clicar — mostra o tooltip session.row.historicalTooltip ('retomar chega na Fase 4') (SESS-01)"
  prohibitions:
    - "MUST NOT renderizar o botão Nova sessão enquanto o estado tool-missing estiver ativo — o usuário nunca deve descobrir a ausência só depois de tentar criar (PROJ-04)"
  artifacts:
    - src/components/session/SessionSidebar.tsx
    - src/components/session/SessionRow.tsx
    - src/components/session/ToolMissingState.tsx
    - src/locales/pt-BR/session.json
    - src/locales/en/session.json
  key_links:
    - "SessionSidebar substitui SidebarPlaceholder via AppShell — mesmo slot 240px fixo da Fase 1"
    - "O status dot reusa statusDotVariants/StatusTone (4 tons); o dot histórico é um modificador CSS outline sobre neutral, não um StatusTone novo"
    - "session-store faz merge incremental das sessões descobertas em sessions[] sem sobrescrever o estado liveSessions das já vivas"
---

<objective>
UI de sessões: a SessionSidebar real (substitui o SidebarPlaceholder e o gatilho temporário do tracer) com descoberta (SESS-01) e a tela de instrução de instalação (PROJ-04). Expande o slice do tracer transformando "criar uma sessão via botão temporário" em "ver as sessões do projeto na sidebar, agrupadas, e criar novas — a menos que as ferramentas estejam ausentes".

Purpose: SESS-01 exige ver as sessões do projeto na sidebar; PROJ-04 exige bloquear a criação e instruir a instalação quando `claude`/gsd-core faltam, antes de qualquer tentativa de spawn.
Output: `SessionSidebar`, `SessionRow`, `ToolMissingState`, expansão do `session-store` para descoberta, swap no `AppShell`, namespaces i18n `session.json`.
</objective>

<execution_context>
@/home/user/gsd-cards/.claude/gsd-core/workflows/execute-plan.md
@/home/user/gsd-cards/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-sess-o-viva/02-UI-SPEC.md
@.planning/phases/02-sess-o-viva/02-RESEARCH.md
@.planning/phases/02-sess-o-viva/02-PATTERNS.md
@src/stores/board-store.ts
@src/shell/AppShell.tsx
@src/shell/SidebarPlaceholder.tsx
@src/components/PhaseCard.tsx
@src/components/StatusBadge.tsx
@src/components/EmptyState.tsx
@src/components/ErrorState.tsx
@src/sessions/discover.ts
@src/dependencies/check.ts
</context>

## Artifacts this phase produces (introduzidos por este plano)

- React: `src/components/session/SessionSidebar.tsx` (header + lista agrupada + empty state + tool-missing state; substitui SidebarPlaceholder)
- React: `src/components/session/SessionRow.tsx` (variantes visuais live/historical/starting/exited via StatusTone; label id-derivado; timestamp relativo; ações no hover no Plano 06)
- React: `src/components/session/ToolMissingState.tsx` (três variantes de cópia, reusa o padrão visual de ErrorState, escopado à coluna da sidebar)
- TS: `src/stores/session-store.ts` (expansão: `sessions[]`, merge incremental da descoberta, estado de tool status)
- React modificado: `src/shell/AppShell.tsx` (swap SidebarPlaceholder → SessionSidebar), `src/shell/DrawerRail.tsx` (remove gatilho temporário; badge de contagem de sessões vivas)
- i18n: expansão de `session.json` (pt-BR/en) — chaves `session.sidebar.*`, `session.empty.*`, `session.row.*`, `session.toolMissing.*`
- CSS: modificador `.status-dot--outline` sobre `status-dot--neutral` (dot da sessão histórica)

<tasks>

<task type="auto">
  <name>Task 1: session-store de descoberta + SessionSidebar + SessionRow (SESS-01)</name>
  <files>src/stores/session-store.ts, src/components/session/SessionSidebar.tsx, src/components/session/SessionRow.tsx, src/locales/pt-BR/session.json, src/locales/en/session.json</files>
  <read_first>
    - src/stores/board-store.ts linhas 358-414 (mergePhaseModels — merge incremental por chave preservando campos), 443-526 (ação async com um único set()), 685-781 (sub-estado tipado)
    - src/components/PhaseCard.tsx (+ PhaseCard.test.tsx colocado — convenção de teste)
    - src/components/StatusBadge.tsx (statusDotVariants/StatusTone — reusar; adicionar modificador CSS outline)
    - src/components/EmptyState.tsx (reuso para zero sessões)
    - src/sessions/discover.ts (listSessions — fonte de dados)
    - .planning/phases/02-sess-o-viva/02-UI-SPEC.md ## Session Sidebar & Row Anatomy, ## Color (mapeamento de status dot), ## Copywriting Contract (session.json), ## UI Considerations (empty/populated/overflow/zero-one-many/long-text)
  </read_first>
  <action>
    Expandir src/stores/session-store.ts com `sessions: SessionDescriptor[]` (id, lastModified, origin: "live"|"historical"), uma ação `discoverSessions(projectRoot)` que chama `register_sessions_scope` (via invoke) + `listSessions` e faz merge incremental em `sessions[]` sem clobbering do estado liveSessions (padrão mergePhaseModels), num único set(). Criar src/components/session/SessionSidebar.tsx: mesma coluna 240px fixa; header com "Sessões" (Heading) + CTA "Nova sessão" (accent, ícone Plus); corpo com dois grupos "Ativas"/"Histórico" (renderizar header do grupo só se não-vazio), cada um ordenado por lastModified desc; EmptyState quando zero sessões. Criar src/components/session/SessionRow.tsx (altura 40px): status dot 8px (mapeamento do UI-SPEC via StatusTone; dot histórico = modificador `.status-dot--outline` sobre neutral), label `"Sessão " + id.slice(0,8)` em JetBrains Mono (Label 12px/600) truncado com ellipsis + `title` com id completo, timestamp relativo via date-fns locale-aware. Row histórica: clique mostra `session.row.historicalTooltip` (toast/hint inline), NÃO abre o drawer; row viva: clique seta activeSessionId. Adicionar as chaves i18n a session.json (pt-BR fonte + en) conforme ## Copywriting Contract. Teste colocado SessionRow.test.tsx (variantes live/historical, truncamento, ordenação).
  </action>
  <verify>
    <automated>npx vitest run src/components/session src/stores/session-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/session/SessionSidebar.tsx e SessionRow.tsx existem e renderizam os grupos Ativas/Histórico
    - session-store expõe `sessions` e `discoverSessions` (grep -q)
    - SessionRow trunca o label e usa um `title` com o id completo (grep -q title SessionRow.tsx)
    - grupo vazio não renderiza header (teste cobre zero-one-many: uma sessão sem o outro grupo)
    - `npx vitest run src/components/session src/stores/session-store.test.ts` sai 0
  </acceptance_criteria>
  <done>Sidebar real lista as sessões descobertas em dois grupos ordenados, com empty state, truncamento e comportamento de row histórica (tooltip, sem abrir drawer).</done>
</task>

<task type="auto">
  <name>Task 2: ToolMissingState (PROJ-04) + swap no AppShell + badge no DrawerRail</name>
  <files>src/components/session/ToolMissingState.tsx, src/shell/AppShell.tsx, src/shell/DrawerRail.tsx, src/locales/pt-BR/session.json, src/locales/en/session.json</files>
  <read_first>
    - src/components/ErrorState.tsx (padrão visual a reusar — ícone de aviso centralizado + heading + body)
    - src/shell/AppShell.tsx linhas 17, 45 (import e uso de SidebarPlaceholder — o slot a trocar)
    - src/shell/DrawerRail.tsx (arquivo do Plano 01 — remover o gatilho temporário; adicionar badge)
    - src/dependencies/check.ts (estado de tool-missing de três variantes)
    - .planning/phases/02-sess-o-viva/02-UI-SPEC.md ## Tool-Missing State (três variantes, escopo à coluna, checado no open/boot), ## Copywriting Contract (session.toolMissing.*), ## Layout Delta (rail badge)
  </read_first>
  <action>
    Criar src/components/session/ToolMissingState.tsx: reusa o padrão visual de ErrorState, escopado à coluna 240px da sidebar (não takeover de página — o board continua funcionando). Três variantes de cópia por o que falta: claude ausente (`session.toolMissing.claude.*`), gsd-core ausente com claude presente (`session.toolMissing.gsdCore.*`), ambos (`session.toolMissing.both.*`). Quando ativo, a SessionSidebar renderiza o ToolMissingState no lugar do corpo da lista e NÃO renderiza o botão Nova sessão. A condição de tool-missing vem de `check.ts` (claudePath) + `has_gsd_core` (do validate); checar no open/reopen do projeto (gsd-core) e no boot/primeira montagem da sidebar (claude), antes de qualquer affordance de criação. Adicionar as chaves session.toolMissing.* a session.json (pt-BR + en) — texto aponta para "consulte a documentação oficial", sem comando/URL específico (ver ## Copywriting Contract nota + assunção flagada abaixo). src/shell/AppShell.tsx: trocar `<SidebarPlaceholder />` por `<SessionSidebar />`. src/shell/DrawerRail.tsx: remover o gatilho temporário do tracer; adicionar o badge de contagem accent no ícone do rail quando ≥1 sessão viva; ao clicar o rail, reexpandir para a última sessão focada.
  </action>
  <verify>
    <automated>npx vitest run src/components/session/ToolMissingState.test.tsx src/shell/AppShell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/session/ToolMissingState.tsx renderiza as três variantes conforme a combinação de flags (teste cobre claude-ausente, gsd-core-ausente, ambos)
    - Quando tool-missing está ativo, o botão Nova sessão não aparece (teste afirma ausência do CTA)
    - src/shell/AppShell.tsx importa SessionSidebar e não mais SidebarPlaceholder (grep -q SessionSidebar; ausência de SidebarPlaceholder)
    - session.json (pt-BR e en) contém as chaves toolMissing.claude / gsdCore / both
    - `npx vitest run src/components/session/ToolMissingState.test.tsx src/shell/AppShell.test.tsx` sai 0
  </acceptance_criteria>
  <done>Tela de instrução de instalação com três variantes bloqueia a criação de sessão quando ferramentas faltam; sidebar montada no shell; rail com badge de sessões vivas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session-store → invoke(register_sessions_scope/listSessions) | a UI consome dados de sessão do backend; nunca assume sessão viva sem confirmar via backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | UI fingir que o app funciona sem claude/gsd-core, levando o usuário a um erro cru de spawn (SessionSidebar/ToolMissingState) | medium | mitigate | checar dependências ANTES de renderizar qualquer affordance de criação; ToolMissingState substitui o corpo da sidebar e esconde o CTA (Pitfall 6) |
| T-02-03 | Spoofing (estado) | UI assumir uma sessão viva sem confirmação do backend (session-store) | low | mitigate | PtyManager (backend) é a única fonte de verdade sobre sessões vivas; a UI só espelha o que o backend reporta (V3 Session Management do research) |
| T-02-SC | Tampering | nenhuma dependência nova (só componentes React sobre a stack existente) | low | accept | sem instalação neste plano |
</threat_model>

## Flagged Planner Assumptions

- **Edge-probe unclassified (SESS-01, PROJ-04):** classificados `unclassified/unresolved`. Critérios de aceite concretos autorados em `must_haves.truths` (verificáveis por vitest). Sem descarte silencioso.
- **UI-SPEC unresolved — cópia de instalação (session.toolMissing.*.body):** a cópia evita deliberadamente um comando/URL específico (poderia envelhecer). Assunção flagada: o executor deve conferir/apertar essa cópia contra a documentação oficial atual de instalação ANTES de shippar, sem mudar os nomes das chaves i18n (02-UI-SPEC.md ## UI Considerations linha `unresolved`).
- **Open Question #1 (research):** exibir só id truncado + timestamp relativo é o escopo MVP; se o UAT rejeitar, extrair título via comando Rust de streaming-read é a extensão segura (não `readTextFile` do arquivo inteiro).

<verification>
- `npx vitest run src/components/session src/stores/session-store.test.ts src/shell/AppShell.test.tsx` verde
- Smoke manual: abrir um projeto com sessões existentes e ver a lista agrupada; simular claude ausente e ver o ToolMissingState sem o CTA
</verification>

<success_criteria>
- Sidebar lista sessões descobertas em grupos ordenados, com empty/overflow/zero-one-many cobertos
- Tool-missing bloqueia a criação e instrui a instalação nas três variantes, sem nunca auto-instalar
</success_criteria>

<output>
Create `.planning/phases/02-sess-o-viva/02-04-SUMMARY.md` when done
</output>
