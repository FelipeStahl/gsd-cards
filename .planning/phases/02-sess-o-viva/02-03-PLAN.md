---
phase: 02-sess-o-viva
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src-tauri/src/dependencies.rs
  - src-tauri/src/sessions.rs
  - src-tauri/src/project.rs
  - src-tauri/src/lib.rs
  - src/dependencies/check.ts
  - src/sessions/discover.ts
  - src/sessions/jsonl-signals.ts
autonomous: true
requirements: [PROJ-04, SESS-01]
must_haves:
  truths:
    - "check_claude_on_path retorna o caminho quando `claude` está no PATH e None quando ausente, via which::which (PROJ-04)"
    - "validate_project_root passa a devolver has_gsd_core, true quando existe .claude/gsd-core no projeto OU no home OU quando gsd-tools resolve no PATH (PROJ-04)"
    - "encode_project_path reproduz exatamente a regra verificada: /home/user/gsd-cards → -home-user-gsd-cards (cada separador vira traço) (SESS-01)"
    - "register_sessions_scope concede escopo de leitura do plugin-fs SÓ para ~/.claude/projects/<encoded-do-projeto-atual>/, nunca a pasta ~/.claude/projects/ inteira (SESS-01)"
    - "listSessions lista apenas *.jsonl do nível raiz da pasta codificada, filtra a subpasta subagents/ e não-recursa, extraindo só id (nome sem .jsonl) + lastModified (mtime) (SESS-01)"
  prohibitions:
    - "MUST NOT auto-instalar o Claude CLI nem o gsd-core — apenas detectar e instruir (REQUIREMENTS Out of Scope + PROJ-04)"
    - "MUST NOT conceder escopo de plugin-fs a ~/.claude/projects/ inteira — isso vazaria metadados de sessões de outros projetos do usuário"
    - "MUST NOT fazer parsing semântico do conteúdo do .jsonl (ler message.content) — usar só como sinal de existência (nome do arquivo = id, mtime = última atividade)"
  artifacts:
    - src-tauri/src/dependencies.rs
    - src-tauri/src/sessions.rs
    - src/dependencies/check.ts
    - src/sessions/discover.ts
    - src/sessions/jsonl-signals.ts
  key_links:
    - "register_sessions_scope reusa o padrão canonicalizar+conceder de project.rs (mesma disciplina de escopo por-projeto da Fase 1)"
    - "has_gsd_core é um campo aditivo em ValidatedProject, calculado durante a mesma validação já testada — não um fluxo paralelo novo"
    - "discover.ts degrada a [] quando a pasta codificada não existe (projeto novo sem sessões), nunca lança"
---

<objective>
Camada de backend + serviços das sessões: detecção de dependências (PROJ-04) e o escopo estreito de descoberta de sessões (SESS-01). Expande o slice do tracer com as duas checagens que precisam existir ANTES de qualquer criação de sessão real na UI, e com o comando que concede — de forma segura — leitura da pasta codificada de sessões do projeto atual.

Purpose: PROJ-04 precisa bloquear a criação de sessão quando `claude`/gsd-core estão ausentes (Pitfall 6: nunca deixar o usuário descobrir a ausência só depois de tentar). SESS-01 precisa listar as sessões existentes sem vazar as de outros projetos (Pattern 6) e sem ler arquivos `.jsonl` gigantes (Pitfall 3).
Output: `dependencies.rs`, `sessions.rs`, `has_gsd_core` em `project.rs`, e os wrappers TS `check.ts`/`discover.ts`/`jsonl-signals.ts`.
</objective>

<execution_context>
@/home/user/gsd-cards/.claude/gsd-core/workflows/execute-plan.md
@/home/user/gsd-cards/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-sess-o-viva/02-RESEARCH.md
@.planning/phases/02-sess-o-viva/02-PATTERNS.md
@.planning/phases/02-sess-o-viva/02-VALIDATION.md
@src-tauri/src/project.rs
@src-tauri/src/lib.rs
@src/planning/phase-scan.ts
</context>

## Artifacts this phase produces (introduzidos por este plano)

- Rust: `src-tauri/src/dependencies.rs` (`DependencyStatus { claude_path: Option<String> }`, comando `#[tauri::command] check_claude_on_path`)
- Rust: `src-tauri/src/sessions.rs` (`fn encode_project_path`, comando `#[tauri::command] register_sessions_scope`, `SessionsError`)
- Rust: `src-tauri/src/project.rs` (campo aditivo `has_gsd_core: bool` em `ValidatedProject`)
- Rust: `src-tauri/src/lib.rs` (registro de `check_claude_on_path` e `register_sessions_scope` no `generate_handler!`)
- TS: `src/dependencies/check.ts` (wrapper `invoke("check_claude_on_path")` + estado de tool-missing)
- TS: `src/sessions/discover.ts` (`listSessions(encodedDir)` — readDir/stat via plugin-fs, filtro defensivo)
- TS: `src/sessions/jsonl-signals.ts` (helpers puros: extrair id + mtime; nunca parse profundo)

<tasks>

<task type="auto">
  <name>Task 1: Detecção de Claude CLI + gsd-core (PROJ-04)</name>
  <files>src-tauri/src/dependencies.rs, src-tauri/src/project.rs, src-tauri/src/lib.rs, src/dependencies/check.ts</files>
  <read_first>
    - src-tauri/src/project.rs linhas 18-25 (struct ValidatedProject), 61-105 (validate_project_root — onde has_gsd_core é calculado), 27-45 (enum de erro)
    - src-tauri/src/lib.rs (generate_handler! — registrar check_claude_on_path)
    - .planning/phases/02-sess-o-viva/02-RESEARCH.md Pattern 5 (detecção de dependências), Code Examples (check_claude_on_path), Assumptions Log A3, Open Question #2
    - .planning/REQUIREMENTS.md ## Out of Scope (auto-instalação proibida)
  </read_first>
  <action>
    Criar src-tauri/src/dependencies.rs com `DependencyStatus { claude_path: Option<String> }` (`#[serde(rename_all = "camelCase")]`) e `#[tauri::command] check_claude_on_path()` retornando `which::which("claude").ok().map(...)` — checagem global, independente de projeto (ver Code Examples do research). Estender src-tauri/src/project.rs: adicionar `has_gsd_core: bool` a `ValidatedProject`, calculado dentro de `validate_project_root` como TRUE se qualquer um: (a) `<root_canonical>/.claude/gsd-core/` existe, (b) `<home>/.claude/gsd-core/` existe (resolver home via PathResolver do Tauri), (c) `which::which("gsd-tools")` resolve (Pattern 5, escopo mínimo v1 Claude-only da Open Question #2 — não checar .codex/.cursor). Registrar `check_claude_on_path` no generate_handler! de lib.rs. Criar src/dependencies/check.ts: wrapper `invoke("check_claude_on_path")` devolvendo `{ claudePath }`, e um helper que combina `claudePath` + `has_gsd_core` (de validate) num estado de tool-missing de três variantes (claude ausente / gsd-core ausente / ambos), consumido pela UI no Plano 04. NENHUMA lógica de auto-instalação.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml check_claude_on_path && cargo build --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/dependencies.rs contém `fn check_claude_on_path` e `which::which` (grep -q)
    - src-tauri/src/project.rs ValidatedProject contém `has_gsd_core` (grep -q has_gsd_core)
    - `cargo test --manifest-path src-tauri/Cargo.toml check_claude_on_path` sai 0 (caso presente → caminho, ausente → None)
    - src/dependencies/check.ts exporta o wrapper de check e o estado de tool-missing (grep -q check_claude_on_path OR checkClaude)
    - Nenhum caminho de código instala binários (revisão: `dependencies.rs` não invoca npm/curl/instalador)
  </acceptance_criteria>
  <done>check_claude_on_path e has_gsd_core funcionam e são testados; o frontend consegue derivar as três variantes de tool-missing sem tentar instalar nada.</done>
</task>

<task type="auto">
  <name>Task 2: Escopo estreito + descoberta de sessões (SESS-01)</name>
  <files>src-tauri/src/sessions.rs, src-tauri/src/lib.rs, src/sessions/discover.ts, src/sessions/jsonl-signals.ts</files>
  <read_first>
    - src-tauri/src/project.rs linhas 47-59 (is_contained/canonicalize), 91-97 (allow_directory por-projeto — padrão a replicar com escopo mais estreito)
    - src/planning/phase-scan.ts linhas 35-42 (parsePhaseDirName puro), 155-237 (scan defensivo: falha por-entrada vira issue, scan degrada a [])
    - .planning/phases/02-sess-o-viva/02-RESEARCH.md Pattern 4 (descoberta), Pattern 6 (escopo estreito), Code Examples (encode_project_path + filtro de listagem), Anti-Patterns
    - .planning/phases/02-sess-o-viva/02-PATTERNS.md seções sessions.rs, discover.ts, jsonl-signals.ts
  </read_first>
  <action>
    Criar src-tauri/src/sessions.rs: `fn encode_project_path(root: &str) -> String` que normaliza `\` para `/` e substitui cada `/` por `-` (verificado: /home/user/gsd-cards → -home-user-gsd-cards); `SessionsError` no estilo enum tagged de project.rs (`NoHomeDir`, `Io`); `#[tauri::command] register_sessions_scope(app, project_root)` que resolve o home do usuário, monta `~/.claude/projects/<encode_project_path(project_root)>/`, e chama `app.fs_scope().allow_directory(&encoded_dir, true)` APENAS para essa subpasta — reusando a disciplina de containment de project.rs; devolve a `encoded_dir` string para o frontend ler. Adicionar `#[cfg(test)] mod tests` com o caso verificado de encode_project_path (e variante `#[cfg(windows)]` para o caminho com `\`). Registrar `register_sessions_scope` no generate_handler! de lib.rs. Criar src/sessions/discover.ts: `listSessions(encodedDir)` via `readDir`/`stat` do @tauri-apps/plugin-fs — filtrar `entry` por `!entry.isDirectory && entry.name.endsWith(".jsonl")` (nunca recursar em `subagents/`), mapear cada um a `{ id: name sem .jsonl, lastModified: mtime }`, com try/catch por-entrada (falha de stat vira skip, não aborta) e degradação a `[]` quando a pasta não existe (projeto novo). Criar src/sessions/jsonl-signals.ts com helpers puros de extração (id do nome do arquivo, ordenação por mtime) — sem qualquer leitura de conteúdo do .jsonl.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml encode_project_path && npx vitest run src/sessions/discover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/sessions.rs contém `fn encode_project_path` e `fn register_sessions_scope` (grep -q)
    - `cargo test --manifest-path src-tauri/Cargo.toml encode_project_path` sai 0 (afirma /home/user/gsd-cards → -home-user-gsd-cards)
    - src/sessions/discover.ts contém `.endsWith(".jsonl")` e NÃO contém readDir recursivo/subagents (grep -q jsonl; ausência de recursão em subagents)
    - `npx vitest run src/sessions/discover.test.ts` sai 0 com fixture que inclui subagents/ e um arquivo não-.jsonl (ambos filtrados)
    - jsonl-signals.ts não lê conteúdo de arquivo (revisão: sem readTextFile/matter)
  </acceptance_criteria>
  <done>encode_project_path e o escopo estreito por subpasta funcionam e são testados; listSessions filtra subagents/ e não-.jsonl, extrai só id+mtime, e degrada a [] sem sessões.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Renderer → register_sessions_scope | project_root cruza aqui — escopo concedido deve ser só a subpasta codificada do projeto |
| Rust → ~/.claude/projects/ | filesystem de outra ferramenta (Claude Code), contendo sessões de outros projetos do mesmo usuário |
| React Renderer → PATH do SO (which) | detecção de binários instalados |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | escopo de plugin-fs amplo demais para ~/.claude/projects/ (sessions.rs) | high | mitigate | register_sessions_scope concede leitura só a ~/.claude/projects/<encoded-do-projeto-atual>/ (Pattern 6), nunca a pasta pai; encode_project_path testado |
| T-02-06 | Information Disclosure | ler o conteúdo do .jsonl (dados de conversa do usuário) além do necessário (discover.ts) | medium | mitigate | descoberta usa só nome do arquivo (id) + mtime; nenhum readTextFile do conteúdo (Anti-Pattern 1 / Pitfall 3) |
| T-02-07 | Tampering | fingir dependências presentes / instalar binários de terceiros silenciosamente (dependencies.rs) | medium | mitigate | apenas detectar (which/existência de diretório) e instruir; auto-instalação é proibida (REQUIREMENTS Out of Scope) |
| T-02-SC | Tampering | crates já instalados no Plano 01 (which); nenhum pacote novo aqui | low | accept | sem instalação nova neste plano — `which` auditado OK em 02-RESEARCH.md |
</threat_model>

## Flagged Planner Assumptions

- **Edge-probe unclassified (PROJ-04, SESS-01):** ambos classificados `unclassified/unresolved`. Critérios de aceite concretos derivados de 02-VALIDATION.md e autorados em `must_haves.truths` (todos verificáveis por teste automatizado — nenhum precisou de backstop). Sem descarte silencioso.
- **A3 (detecção de gsd-core):** modelar "gsd-core instalado" como existência de diretório OU `which gsd-tools` é inferência da pesquisa (sem comando "doctor" oficial); reversível e isolado em dependencies.rs/project.rs (Assumptions Log A3).
- **Open Question #2:** escopo mínimo v1 Claude-only — não checar `.codex/`/`.cursor/` (RUN-01 é v2).

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml` verde (check_claude_on_path, encode_project_path)
- `npx vitest run src/sessions` verde (discover com fixture subagents/ + não-.jsonl)
</verification>

<success_criteria>
- Detecção de `claude`/gsd-core funcional e testada, sem qualquer caminho de auto-instalação
- Escopo de descoberta estreito por subpasta codificada; listagem filtra subagents/ e não-.jsonl, extrai só id+mtime
</success_criteria>

<output>
Create `.planning/phases/02-sess-o-viva/02-03-SUMMARY.md` when done
</output>
